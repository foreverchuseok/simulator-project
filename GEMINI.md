# Google Antigravity 프로젝트 진입점

작업 전에 루트 `AGENTS.md`를 읽고 공통 규칙을 따른다.
프로젝트 구조와 기능별 수정 위치는 `README.md`를 참고한다.
Blender 작업은 `blender/BLENDER-WORKFLOW.md`를 추가로 읽는다.

## 영상·이미지 분석 게이트

영상, 이미지, 도면 또는 승강기 메커니즘 분석을 요청받으면 다음 순서를 지킨다.

1. 코드를 수정하지 않고 자료와 현재 코드를 먼저 분석한다.
2. 분석 결과를 루트 `PLAN.md`에 작성한다.
3. 사용자에게 다음 중 무엇을 할지 확인한다.
   - 이 계획대로 Antigravity가 직접 구현한다.
   - 구현하지 않고 `PLAN.md`만 남겨 Claude Code 또는 Cursor에 전달한다.
4. 사용자의 선택을 받은 뒤에만 구현한다.

처음부터 단순한 코드 수정을 직접 요청받은 경우에는 계획 파일 없이 바로 구현할 수 있다.
`PLAN.md`는 현재 작업 하나만 담고, 완료된 과거 계획을 재사용하지 않는다.
