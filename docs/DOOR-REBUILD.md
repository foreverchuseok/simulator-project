# DOOR-REBUILD — 도어 계통 재공사 기록

2026-08-17에 카문·승장문과 그 주변 부품을 화면에서 걷어내고, 부품별로 다시 올리기 위해 남긴 기록이다.
Claude Code, Google Antigravity, Codex, Cursor는 도어 작업을 시작하기 전에 이 문서와 `js/archive/doors.js`를 먼저 읽는다.

앱은 이 원본을 로드하지 않는다. `index.html`에 `js/archive/doors.js`를 추가하지 않는다.

## 현재 화면 상태

- 카 도어 복원(2026-09-19): `js/car-door.js`에 카문·행거·모터/벨트·CDL 클러치/카도어락·스위치·해제 와이어를 올렸다. 상세 계약과 검증은 [CAR-DOOR.md](CAR-DOOR.md)를 따른다. 카문 구동값에서 물린 층의 승장문 위치를 계산하며, 아래의 과거 카문 스텁 및 승장문 독립 트윈 설명을 대체한다.

- 렌더링 최적화(2026-09-18): 실 서포트·실·토가드·삼방틀의 이름 없는 고정 메시만 `batchStaticChildren()`으로 묶는다. 층별 부모 위치·그룹과 표시기 텍스처를 유지하고, 행거·도어·인터록·삼각키 가동부는 이 묶음에 포함하지 않는다. 상세 결과는 [공유 로드맵](ROADMAP.md)의 렌더링 2차 기록을 참고한다.

걷어낸 것:

- 카 도어 패널, 유리창, 베젤, 선단 세이프티슈, 가이드슈, 후면 리브
- 카 도어 클러치(벌림형 베인)
- 카 도어 오퍼레이터(흑색 헤더, 모터, 노란 구동 풀리, 아이들러, 타이밍 벨트, 행거)
- 승장 도어 패널, 삼방틀(jamb)·트랜섬, 층표시 LED
- 승장 도어 헤더(행거 레일, 연동로프·풀리, 도어추, 삼각키)
- 승장 행거·인터록(후크, 적층 롤러, 돼지발 접점)
- 페시아 플레이트

다시 올린 것:

- 승장 실 서포트(Sill Support) 브라켓 — `js/elevator.js` `buildHatchDoors()` 1차·2차 브라켓 및 볼트 어셈블리 (전 층 5개씩 등간격 설치)
- 승장 이중실 — `js/elevator.js` `createHallSill()` (홀 가장자리는 기존 유지, 승강로 −Z로만 살짝 확장)
  - **1번 홈** (`SILL_GROOVE1_Z`): 문짝 아래. 일반 가이드 슈 2개소가 왕복.
  - **2번 홈** (`SILL_GROOVE2_Z`): 승강로 쪽 보강실. 문짝당 보강슈 1개가 왕복 (450J 이탈 방지).
  - 홈 바닥은 막힘. 로비 대리석과 실 사이 개구 바닥은 슬래브로 잇는다.
  - Z 원본은 `DOOR_HALL_Z` / `SILL_Z` / `SILL_GROOVE1_Z` / `SILL_GROOVE2_Z`.
- 토가드(실 커버) — `js/elevator.js` `createToeGuard()` 실 서포트 5곳에 결합
  - 수직 270mm, 폭은 실과 동일. 하단은 벽(+Z) 쪽으로 굽힘(수평 대비 60°, 길이 35mm ≥ 20mm).
  - 승강로에서 보면 브라켓 앞을 가린다.
- 승장 삼방틀(Jamb) & 트랜섬(Transom) — `js/elevator.js` `createJambAssembly()` + 로비 문틀 `js/environment.js` `buildFrontWallAndLobby()`
  - 헤어라인 스테인리스 `M.silverHairline(0xc8d0d8)`. 올리브 기둥·테라코타 상인방 장식은 제거.
- 승장 도어 오퍼레이터(행거 케이스 ASSY) — `js/elevator.js` `createHangerCaseAssembly()` (부품설계.pdf 173~174p)
  - 양단 L브라켓이 승강로 벽 앙카 + 상·하 리턴 + M10으로 C레일을 받침
  - C형 행거 케이스 104×35mm, 트랙센터 피아노선에서 69mm
  - **전폭 `hcW` 는 도어 행정에서 역산한다**: `2 * (ox + HP_W/2 + 0.075)` ≈ **2.82m**
    (2짝 중앙개폐 행정 ±1.145 + 행거판 반폭 0.19 + 여유). 예전 고정값 `S.DOOR_W + 0.40` = 1.90m 은
    행정보다 좁아서 문을 열면 행거판과 연동로프 고정단이 레일 밖 허공으로 튀어나갔다.
    ★`ox` 를 건드리면 헤더 폭·연동 풀리 위치·클로저 커버 길이가 전부 따라 움직인다.
  - 연동 풀리는 케이스 양단(`±(hcW/2 - 0.115)` ≈ ±1.295), 레일 높이
- 현장 실사 인터록 GLB — 2026-09-09 사진 5장과 `20260909_094039.mp4` 기준.
  - 원본 `blender/scripts/hall_interlock.py` → `models/gltf/hall_interlock.glb` → `js/hall-interlock.js`.
  - 스위치는 헤더 고정, 가동 걸쇠·롤러는 +X 문짝, 긴 사각 구멍 걸쇠걸이는 −X 문짝에 장착한다.
  - 투명 커버·흰 절연체·배선·보조접점·황록/연분홍 도금 판금·관통 슬롯·나선 스프링을 포함한다. 현대 로고 스티커는 만들지 않는다.
  - 아래 **현장 인터록 GLB 계약**이 이전의 JavaScript 형상·고정 포켓 설명을 대체한다.

- 승장 도어 좌·우 행거 플레이트 어셈블리 — `js/elevator.js` **`buildHangerAssembly(grp, side, cx, g)`**
  (도면 173~174p + 특허 KR200462913Y1 + 실사 211020.png, 001752.png, 001821.png). 좌·우를 두 벌로 쓰지 않고 `side` 부호로 미러한다.
  - 공통: 크로메이트 베이스판(380×160×3.5) + C레일 주행 롤러 2개 + 이탈방지 롤러 1개 + 하단 도어 체결 플랜지/M8 볼트 2세트
  - **월드 +X 패널(화면 왼쪽, `right`)**: GLB의 `InterlockBase`와 `Hook`. 상부 고정 롤러, 하부 가동 롤러, 스프링 심봉·와셔·조정 너트, 접점 절연체, 사각 턱, 삼각키 평철 링크를 포함한다.
  - ★**양단 릴레이 풀리 규격**: `ROPE_R = 0.022`, `PUL_R = 0.030`.
    **풀리 홈 반지름이 상·하 가닥 간격(2·ROPE_R = 44mm)을 정하는 단일 원본이다.**
    행거판은 높이 160mm·중심 `caseCY-40mm` 라 윗변이 `caseCY+40mm` 다. `ROPE_R` 이 40mm 면
    상부 가닥이 윗변에 정확히 올라타 판 밖으로 튀어나오고, 플랜지 46mm 는 C레일 상·하 립(±38mm)을 뚫는다.
    22mm 로 줄여야 두 가닥이 판 높이 **안쪽**으로 들어와 판이 앞을 가릴 수 있다(사용자 지시 2026-08-25).
  - ★**연동 로프 Z 는 상·하 분리하지 않는다 — 한 평면 `ROPE_PLANE_Z = HP_PLATE_Z + 0.012`**:
    - 상·하 가닥 모두 **행거판 뒤 12mm**. "Z 를 나눠서 하부만 숨긴다"가 아니라 **판이 앞을 막는다**가 규약이다.
    - 12mm 는 `판 반두께 1.75mm + 풀리 반두께 11mm` 에서 나온다. 8mm 로 잡으면 활짝 열렸을 때
      행거판 바깥변이 풀리를 덮는데도 풀리 앞 플랜지가 판 앞면을 1.25mm 뚫고 나온다.
    - 로프 본선이 보이는 곳은 **행거판 사이·바깥의 열린 헤더 구간**이다.
      −X 판에서는 두 관통창으로 짧은 종단 리드만 판 앞(승강로 쪽)으로 나온다.
  - **월드 −X 패널(화면 오른쪽, `left` 그룹)**:
    - 2026-09-11 사용자 영상 기준으로 작은 2열 수평 풀리와 스프링 텐셔너를 제거했다.
    - 관통창 2개, 중앙 ㄱ자 브라켓과 서로 반대 방향에서 들어오는 전산볼트 종단 2개를 둔다.
    - 압착 슬리브·나사봉·양면 너트로 고정하며 상·하 종단 중심 간격은 26mm다.
    - 본선은 판 뒤의 같은 Y/Z를 유지하며 고정 리드만 창을 통해 높이와 Z를 전환한다.
    - GLB `Keeper`의 보조접점 핀 2개가 고정 스위치 측 소켓 구멍으로 들어간다.
    - 긴 걸쇠걸이는 반대편 문짝의 연장판에 체결되며, 닫힐 때 가동 걸쇠의 사각 턱을 받는다.
  - 헤더 좌표 `caseCY`·`plateZ`·`trackCtrZ`만 JS에서 전달하고 인터록 세부 제원은 GLB extras에서 읽는다.
  - 전 층 `hatchDoors[i].left` 및 `hatchDoors[i].right` 그룹에 마운트되어 도어 개폐 FSM과 동기 이동

- 승장 도어 연동(릴레이팅) 로프 — `createHangerCaseAssembly()` + `spinDoorDrive()`.
  - 2026-09-11 사용자 녹화와 엘박사 연동로프 교체 영상 기준. 치수는 기존 판에 맞춘 시각 재구성이다.
  - +X판은 상부 본선의 단순 클램프, −X판은 하부 본선의 대향 전산볼트 종단 2개다.
  - 양단 반환 풀리·인터록 위치와 GLB·기존 긴 폐문 스프링을 유지한다.
  - 본선 4마디, 양단 180° 감김과 행거판 고정 리드 2개로 구성한다.
  - 본선 Z는 `HP_PLATE_Z + 0.012`, 상·하 간격은 `2*ROPE_R = 44mm`. 고정 리드만 판 앞으로 나온다.
  - 시각 로프 지름은 3.6mm이며 원본은 `js/elevator.js`의 `ROPE_RD`다.
  - 상부 고정 구간은 `2*RA_HALF = 44mm`, 하부 중단점은 `RB_L = -0.125`, `RB_R = 0.125`다.
  - 길이 보존: `upL.scale.y + upR.scale.y + 2*aHalf = pulRX-pulLX`. 하부는 `loL.scale.y + loR.scale.y + bR-bL = pulRX-pulLX`.
  - 본선 Y/Z에 종단 높이차를 적용하지 않는다. 전환은 고정 리드만 담당한다.
- 승장 도어 스프링 클로저 ASSY — `createHangerCaseAssembly()` & `buildHangerAssembly()` (부품설계.pdf 181p)
  - C레일 상단 **+X 끝단** `SPRING CLOSER BRKT` (고정 브라켓, `scbX = hcW/2 - 0.16`)
  - C레일 상부 ㄷ자형 아연도금 보호 덮개 `COVER` — 스프링이 활짝 열림까지 늘어나는 전 구간을 덮되
    전면(승강로 쪽)은 짧은 립만 두어 스프링이 보이게 한다
  - 수평 인장 롱 코일 스프링 `SPRING` — 헬릭스 튜브 1개(`closerCoilGeo`, 단위길이 1m)를
    `mesh.scale.x` 로 늘린다. 늘리면 코일 피치가 같이 벌어져 실제 인장 스프링처럼 보인다.
    닫힘 1.685m → 열림 2.439m (×1.45). 도면 181p 적색 양방향 화살표가 이 신축이다.
  - `left` 그룹(월드 −X 판) 상단 `SPRING HANGER` 체결 러그가 스프링 −X 끝을 끌고 다닌다

- 승장 도어 패널 본체 어셈블리 — `js/elevator.js` **`buildHatchDoorPanel(grp, side, cx, g)`** (도면 175~176p)
  - 프리미엄 실버 헤어라인 스테인리스 패널 (폭 775mm × 높이 2158mm × 두께 32mm)
  - 행거 하단 L플랜지 ↔ 상단 탭 플레이트 & 조절 심 라이너(Liner) M8 볼트 결합
  - 중앙 맞물림 턱 & 흑색 고무 완충 스트립 (완전 닫힘 시 중앙 **5mm** — 안전기준 7.6.2.2.1 손끼임 방지. `DOOR_MEET_GAP`)
  - 승강로 측 후면 세로 보강 C채널 2줄 (폭 50mm, 높이 dh * 0.94)
  - 하단 1번 실 가이드 슈 2개소 + 2번 실 보강슈(문짝당 1개, 승강로 쪽으로 돌출해 홈을 탐)
  - 승강장 홀 의장면 안전 스티커 (손대지 마시오 `hand.png` / 기대면 추락 위험 `lean.png`)
  - 홀 의장면은 `+Z`(`DOOR_HALL_Z` = 벽 내측에서 22mm 승강로 쪽). 승강로 후면·보강 리브·리테이너는 `−Z`.
    예전 `FRONT_INNER_Z` 는 벽보다 ~58mm 안쪽이라 로비에서 실 터널이 넓게 보였다. 행거판보다 승강로 쪽으로 빼서 스티커가 승강로를 향하게 두지 않는다.

- 승장 삼각키 비상 해정 장치 및 인터록 연동 기구 — `js/elevator.js` `buildHatchDoorPanel()` & `buildHangerAssembly()`
  - 승강장 바닥 기준 법정 검사기준 설치 높이 **2.000m** (`triY = 2.000`)
  - 승강장 홀 의장면 베젤 & 정삼각형 키 홀(변 9mm) 및 내부 삼각 황동 핀 코어
  - 도어 패널(두께 32mm) 관통 황동 바디 + 승강로 배면 고무 와셔 및 체결 육각너트 2개 (도면 177p)
  - 회전 캠 레버(Cam Lever, 초기각 35°, 회전반경 45mm) 및 끝단 드라이브 핀
  - **인터록 평철 링크**: GLB의 `LinkStock`와 관통 장공 `LinkFoot`. 상부 핀과 삼각키 캠을 기준으로 배치한다. 링크 길이는 일정하고 캠 핀은 하부 장공 안에서 미끄러진다.
  - 키의 기존 높이·회전 범위는 유지하며, X 위치만 GLB 상부 링크 핀에서 역산한다. `setEmergencyKey()`는 GLB의 해정각을 사용한다.
  - **단일 PLUMB 축 일체화 계약 (도면 174p)**:
    - `HATCH_PLUMB_Z = SILL_Z = DOOR_HALL_Z - HATCH_DT / 2`
    - C레일 트랙 중심 = 행거판 하단 34mm L-플랜지 = 도어 패널 본체 = 승장 실 1번 홈 = 단일 수직 축
    - 벽체 L-브라켓 돌출 깊이(`spanZ`)를 약 22.5mm로 대폭 축소하여 승강로 전면 벽체에 단단하게 밀착
    - 허공 선반을 완전 삭제하고 행거판 하단 34mm L-플랜지와 M8 볼트 2세트가 도어 상단 탭 플레이트/심 라이너와 직접 일체 결합

카 도어 클러치와 오퍼레이터는 2026-09-19 복원했다. 전자 제어 보드의 내부 제어와 장애물 감지는 이번 구현 범위 밖이다.

그대로 둔 것:

- 로비 전면벽 개구부, 출입 프레임, 홀버튼, 점자 — `js/environment.js` `buildFrontWallAndLobby()`
- 도어 치수 `S.DOOR_W` 1.50, `S.DOOR_H` 2.10 — `js/config.js`
- 운행 FSM `openDoors()` / `closeDoors()`는 `CarDoor.open()` / `CarDoor.close()`를 호출한다. 카문 위치에서 승장문 위치를 먼저 갱신한 뒤 `spinDoorDrive(h)`를 호출한다.
- 스티커 PNG `assets/bg/hand.png`, `assets/bg/lean.png` — 삭제 금지

카 에이프런·카 실·체대는 카 재공사로 `js/archive/car.js` / `docs/CAR-REBUILD.md` 로 옮겼다.

`buildCarDoors()`는 `CarDoor.build()`를 호출한다. `buildHatchDoors()`가 먼저 실·삼방틀·행거 케이스와 좌·우 행거 플레이트·승장 도어 패널을 올리고, 카도어가 GLB 롤러 위치를 읽어 조립한다.

## 행거 케이스 계약 (에이전트 필수)

사용자 영상(2026-08-17 183216) 음성이 이 구간의 원본이다. 도면 173~174p와 같다. 코드를 손대기 전에 아래를 따른다.

구조 한 줄:

**벽 앙카 L브라켓(양 끝) → C형 행거 케이스(레일).** 행거판·롤러는 문짝과 같이 올린다. 그때 롤러는 C 궤도 안으로 들어간다.

인터록 스위치 박스는 C레일 앞 고정이다. 노란 GLB 판을 공중에 띄우지 않는다.

헤더 전폭은 **고정 상수가 아니라 도어 행정에서 파생**된다(`hcW = 2*(ox + HP_W/2 + 0.075)` ≈ 2.82m).
활짝 열린 행거판이 레일 위에 남아야 하고 연동로프 고정단이 양단 풀리 안쪽에 있어야 하기 때문이다.
헤더가 삼방틀(1.82m)보다 훨씬 넓게 벽 속으로 뻗는 것은 2짝 중앙개폐에서 정상이다. 좁히지 않는다.

| 맞는 것 | 틀린 것 |
|---|---|
| 브라켓이 헤더 **양 끝단**에 있다 (개구부 바깥, 잠 기둥 쪽) | 개구부 한가운데만 보고 브라켓이 없다고 판단 |
| L브라켓이 승강로 벽과 C웹 사이 간극을 채운다 | C레일만 두고 벽과 떨어진 채 떠 있게 둔다 |
| 지금 화면에는 행거판·롤러가 없다 | 문짝 없이 판 2장·롤러를 C레일에 미리 달아 둔다 |
| 주행 롤러는 C 궤도 `z=0` 안에 건다 | 롤러를 레일 옆·앞에 따로 한 쌍 더 둔다 |
| 연동로프 본선은 판 뒤, −X판의 고정 리드만 관통창 2개를 통해 판 앞 전산볼트 종단으로 연결한다 | 본선 전체를 판 앞으로 옮기거나 리드가 솔리드 판을 뚫는다 |
| 상·하 가닥 간격은 `2·ROPE_R`(44mm)로 행거판 높이 **안쪽**이다 | `ROPE_R` 을 키워 상부 가닥이 판 윗변 위로 올라타게 둔다 |
| 커버 안에 **ㅓ자 흰 하우징**이 보인다. 중간 볼트 2개, 끝 단자 2+2 가로 일렬, 노란 전선, 돼지발 돌출 | 투명 껍데기만 두고 내부를 빼거나, 단자를 위아래로 세운다 |

하지 말 것:

- `models/gltf/hall_interlock.glb`를 `buildHatchDoors()`에서 다시 로드하지 않는다. 파일은 유지한다.
- 도면 등각투영의 별도 프레임 세트·덮개를 이번에 만들지 않는다.
- 벽 쪽에만 넓은 백플레이트를 두고 C레일과 레일 높이에서 연결하지 않는 고정을 만들지 않는다.
- 브라켓을 트랜섬 높이(레일보다 위)에만 두지 않는다. 브라켓 Y는 C레일 중심(`caseCY`)이다.

검증:

- Live Server 하드 리프레시 후 `?doorcam=1` 로 **헤더 양 끝**을 본다. 가운데만 보면 브라켓·연동 풀리가 안 보인다.
- 노란 인터록 판이 공중에 있으면 잘못된 상태다.
- 문을 열어 본다. 행거판이 레일 끝을 벗어나거나 연동로프 고정단이 풀리를 넘어가면 `hcW` 파생이 깨진 것이다.
- 로프 길이 보존 검증 (콘솔):
  `k=hatchDoors[1].link; k.seg.upL.scale.y + k.seg.upR.scale.y + 2*k.aHalf` 가
  닫힘·열림 어느 위치에서도 `k.pulRX - k.pulLX`(2.59) 와 같아야 한다.
  하부 가닥은 고정단이 비대칭이라 `loL.scale.y + loR.scale.y + (k.bR - k.bL)` 로 본다.
- 자동 검증: `node tools/verify_relay_rope.mjs`. 자체 임시 HTTP 서버와 새 Chromium을 사용한다(CDN 접근 필요).
  전 층 개방 0~100%의 11단계에서 길이·접선·종단 연결·관통창·폐문 스프링·인터록 고정 위치를 확인한다.
- 화면: `.shot-relay-new/`의 `closed.png`, `half.png`, `open.png`, `terminal.png`, `back.png`, `pulley.png`.
  수치 결과는 같은 폴더의 `report.json`. 전개 상태에서 양단 판과 풀리 앞뒤 배치도 확인한다.
코드 위치: `js/elevator.js` `createHangerCaseAssembly()`, 갱신은 `spinDoorDrive()`.

## 현장 인터록 GLB 계약 (2026-09-09)

사용자가 제공한 사진 `20260909_093846/093942/093943/094011/094013.jpg` 및 영상 `20260909_094039.mp4`가 외형·부착 관계의 기준이다. 영상 6~8초에서 반대 문짝의 긴 걸쇠걸이와 헤더에 남는 스위치를 확인할 수 있다. 이전의 고정 16mm 포켓 블록은 사용하지 않는다.

| GLB 노드 | 부착·동작 |
|---|---|
| `SwitchAssembly` | 헤더 고정 스위치·투명 커버·받침판·고정 접점. |
| `InterlockBase` | +X 문짝의 장착판·상부 고정 롤러·스프링 가이드. |
| `Hook` | +X 문짝의 가동 걸쇠·하부 롤러·흰 접점부. 피벗을 중심으로 회전. |
| `Keeper` | −X 문짝의 긴 걸쇠걸이·사각 관통 구멍·보조접점. |
| `Spring` / `SpringSeat` / `SpringBottomWasher` | 단일 나선 코일. 심봉 축을 유지하며 레버 높이에 따라 압축. |
| `LinkStock` / `LinkFoot` / `LinkPin` | 일정 길이의 평철과 하부 캠 핀 장공. |

- `T(x,y,z)=(x,-z,y)`, 미터 단위, glTF Y-up, scale=1. GLB X는 완전 닫힘 어셈블리의 X, Y는 트랙 중심 기준, Z는 행거판 중심 기준이다.
- 래퍼: 헤더 `FieldInterlockFixed`, +X 문짝 `FieldInterlockMoving`, −X 문짝 `FieldInterlockOpposite`. 문짝 래퍼의 X에는 닫힘 중심값의 반대 부호를 적용한다.
- `InterlockRoot.userData.contractVersion=2`. `latch`·`spring`·`link` extras가 치수와 애니메이션의 단일 원본이다. JS에 각도·피벗을 중복 입력하지 않는다.
- 가동 턱 폭 20mm, 턱 하단 중앙 기준 물림 8mm, 해정 후 간극 4mm, 개방측 여유 4.5mm, 반대쪽 회전 여유 3mm가 모델 기준값이다. 평철 걸쇠걸이는 3mm 두께, 구멍 Z 폭 10mm, 가동판 두께 6mm이다. 실제 형상과 관통 홈은 `LIP_X`·`LIP_W`를 공유한다.
- 정확한 회전 변환으로 12mm 리프트를 푼 `h.latch.liftRad`를 내보낸 GLB에서 읽는다. 해정각은 `PIVOT`을 옮기면 따라 바뀌므로 JS나 문서에 각도를 따로 적지 않는다.
- 2026-09-17: 영상 9.9초의 노출된 후크를 기준으로 상단 직선, 아래쪽 곡선, 짧고 둥근 갈고리 끝과 세로 링크 탭을 재구성했다. 글자 없는 검정·초록 명판 대체 메시를 제거했다. 41개 회전 자세에서 실제 후크 메시와 걸림판·진입 경사판의 관통 여부를 검사한다.
- 2026-09-12: 롤러·피벗·장착판 끝단을 본체에서 22mm 더 외측(+X)으로 옮겼다. 현장 사진 `093846`(정면)과 `093943`(원근보정)을 롤러 상하 피치 47mm로 축척을 잡아 재면 하부롤러→스프링 49~58mm, 하부롤러→스위치 커버 117mm인데 모델은 32/96.5mm였다. 스프링→스위치 커버는 이미 맞았으므로 이 끝단만 짧았다. 조정 노브는 `hall_interlock.py`의 `OUT` 한 곳이며 +X쪽 상수가 전부 여기서 파생된다.
- `hallLatchPocket`은 반대 문짝의 구멍 입구, `hallLatchKeeperLip`은 가동 턱 하단 마커다. 월드 좌표로 물림을 재며 회전한 판의 AABB로 대신하지 않는다.
- `HallInterlock.updateAll()`은 스프링과 링크의 변환만 갱신한다. 렌더 루프에서 메시·재질·기하를 생성하지 않는다. GLB는 한 번 로드하고 층별 메시 기하·재질은 공유한다.
- 사진에는 치수 기준자가 없으므로 외형·두께·색상은 교육용 재구성이다. 위 수치를 실제 제품의 설치·검사 허용값으로 해석하지 않는다. 카·도어 치수 `const S`는 변경하지 않았다.

검증:

- `node tools/verify_interlock_field.mjs`: 자체 로컬 서버에서 4개 층의 물림·해정, 관통 구멍, 스프링 압축, 평철 길이, 문짝 부착 관계, 개방 행정의 간섭, 삼각키·복귀와 화면을 확인한다. CDN 접근이 필요하다.
- `blender/scripts/render_hall_interlock.py`: 내보낸 GLB를 다시 읽어 Blender 정면·해정 렌더를 확인한다.
- 산출물은 `.shot-interlock-field/`. 로컬 참고 영상 프레임은 `.shot-interlock-ref/`에 두며 앱에서 로드하거나 Git에 올리지 않는다.
- 승강로 −Z에서 보면 화면 왼쪽은 월드 +X, 화면 오른쪽은 월드 −X이다. 사진에 맞춘다고 좌우 부호를 뒤집지 않는다.

## 원본 코드 위치

파일: `js/archive/doors.js`

| 구간 | 함수·위치 | 부품 |
|---|---|---|
| 스티커 로더 | `getStickerMats()` | hand.png / lean.png 재질 |
| 카문 패널 | `buildCarDoors()` §1 `makeDoor` | PVD 티타늄 패널, 유리, 스티커, 슈 |
| 클러치 | `buildCarDoors()` §2 | 우측문 벌림형 베인 |
| 오퍼레이터 | `buildCarDoors()` §3 | 헤더, 모터, 풀리, 벨트 |
| 카 행거 | `buildCarDoors()` §4 | LM 캐리지, 벨트 클램프 |
| 개폐 연동 | `spinDoorDrive()` | 풀리·연동로프·도어추 |
| 승장문 패널 | `buildHatchDoors()` `makeHatchDoor` | 실버 헤어라인, 스티커 |
| 삼방틀·트랜섬 | 같은 함수 층 루프 | jamb, transom, 층표시 LED |
| 승장 헤더 | `═══ H. 승장 도어 헤더` | 헤더판, 레일, 연동로프 |
| 인터록 | 우측 패널 + 헤더 고정부 | 후크, 롤러, 돼지발 |
| 홀실·토가드 | 층 루프 하단 | 이중실, 보강슈, 토가드 270mm |
| 페시아 | `buildHatchDoors()` 루프 뒤 | 층간 수직판 |

재질 원본:

- 카문: `M.pvdTitanium()` 기본 `0xd8cfc2`
- 승장문: `M.silverHairline(0xdfe4ec, 0.28)`
- 에이프런: `M.paint(0x5a3230)` 브라운

스티커 붙임:

- 카문 실내측: 좌문 `lean.png`, 우문 `hand.png`, 패널 안쪽면, 크기 0.132m
- 승장문 로비측: 좌문 `hand.png`, 우문 `lean.png`

## 부품을 다시 올릴 때

1. 이 문서와 `js/archive/doors.js`에서 해당 구간만 읽는다.
2. `js/elevator.js`의 스텁 `buildCarDoors()` 또는 `buildHatchDoors()`에 그 구간만 넣는다.
3. 요청하지 않은 다른 도어 부품을 한꺼번에 복원하지 않는다.
4. 과거 형태를 참고하되, 사용자가 새 자료를 주면 새 자료가 우선이다.
5. 스티커 PNG와 `const S` 도어 치수는 확인 없이 바꾸지 않는다.
6. `index.html`에 아카이브 파일을 연결하지 않는다.

운행 계약(복원 전까지 스텁이 지킨다):

- `carDoorL` / `carDoorR` 과 `hatchDoors[i].left` / `.right`
- `userData.cx`, `userData.ox`
- `spinDoorDrive(h)` — **더 이상 no-op 이 아니다.** 승장 연동로프 마디·양단 풀리 회전·폐문 스프링 길이를
  `h.link` 핸들로 갱신한다. `h.link` 가 없으면 즉시 return 하므로 카 오퍼레이터 복원 시에도 안전하다.
  `CarDoor.pose()`에서 승장 행거판 위치를 갱신한 직후 호출한다. 카문/승장문/로프의 갱신 순서를 바꾸지 않는다.
- `h.hook`, `h.triKey`, `h.doorWeight`, `h.relPulley` 는 없어도 `ui.js`가 통과한다

## 관련 파일

- 치수: `js/config.js` `S.DOOR_W`, `S.DOOR_H`
- 카 에이프런·카실: `js/archive/car.js` `platformGrp` (카 재공사 원본)
- 로비 개구: `js/environment.js` `buildFrontWallAndLobby()`
- 개폐 FSM: `js/ui.js` `openDoors()`, `closeDoors()`
- 음원: `sound/door_open.wav`, `sound/door_close.wav`, `sound/door_closing_voice.mp3`
