# DOOR-REBUILD — 도어 계통 재공사 기록

2026-08-17에 카문·승장문과 그 주변 부품을 화면에서 걷어내고, 부품별로 다시 올리기 위해 남긴 기록이다.
Claude Code, Google Antigravity, Cursor는 도어 작업을 시작하기 전에 이 문서와 `js/archive/doors.js`를 먼저 읽는다.

앱은 이 원본을 로드하지 않는다. `index.html`에 `js/archive/doors.js`를 추가하지 않는다.

## 현재 화면 상태

걷어낸 것:

- 카 도어 패널, 유리창, 베젤, 선단 세이프티슈, 가이드슈, 후면 리브
- 카 도어 클러치(벌림형 베인)
- 카 도어 오퍼레이터(흑색 헤더, 모터, 노란 구동 풀리, 아이들러, 타이밍 벨트, 행거)
- 승장 도어 패널, 삼방틀(jamb)·트랜섬, 층표시 LED
- 승장 도어 헤더(행거 레일, 연동로프·풀리, 도어추, 삼각키)
- 승장 행거·인터록(후크, 적층 롤러, 돼지발 접점)
- 토가드
- 페시아 플레이트

다시 올린 것:

- 승장 실 서포트(Sill Support) 브라켓 — `js/elevator.js` `buildHatchDoors()` 1차·2차 브라켓 및 볼트 어셈블리 (전 층 5개씩 등간격 설치)
- 승장 실(Hall Sill) — `js/elevator.js` `buildHatchDoors()` 2열 도어 가이드 슈 홈 및 미끄럼방지 리브, M8 결합 볼트/너트 어셈블리 (전 층 실 서포트 상단 안착)
- 승장 삼방틀(Jamb) & 트랜섬(Transom) — `js/elevator.js` `createJambAssembly()` + 로비 문틀 `js/environment.js` `buildFrontWallAndLobby()`
  - 헤어라인 스테인리스 `M.silverHairline(0xc8d0d8)`. 올리브 기둥·테라코타 상인방 장식은 제거.
- 승장 도어 오퍼레이터(행거 케이스 ASSY) — `js/elevator.js` `createHangerCaseAssembly()` (부품설계.pdf 173~174p)
  - 양단 L브라켓이 승강로 벽 앙카 + 상·하 리턴 + M10으로 C레일을 받침
  - C형 행거 케이스 104×35mm, 트랙센터 피아노선에서 69mm
  - **전폭 `hcW` 는 도어 행정에서 역산한다**: `2 * (ox + HP_W/2 + 0.075)` ≈ **2.82m**
    (2짝 중앙개폐 행정 ±1.145 + 행거판 반폭 0.19 + 여유). 예전 고정값 `S.DOOR_W + 0.40` = 1.90m 은
    행정보다 좁아서 문을 열면 행거판과 연동로프 고정단이 레일 밖 허공으로 튀어나갔다.
    ★`ox` 를 건드리면 헤더 폭·연동 풀리 위치·클로저 커버 길이가 전부 따라 움직인다.
  - 연동 풀리는 케이스 양단(`±(hcW/2 - 0.115)` ≈ ±1.295), 레일 높이
- 황금색 실사 인터록 앗세이 — 화면에서 뺌 (영상: 일단 빼고, 공중 판 삭제)
  - GLB `models/gltf/hall_interlock.glb` 파일은 유지, `buildHatchDoors()`에서는 로드하지 않음
- 인터록 스위치 박스 — `createHangerCaseAssembly()` `ilMount` (실사 `001216.png` + 영상 `001849.mp4`)
  - C레일 상단 M6 볼트 플랜지 → 크로메이트 백플레이트(검정 고무 배선 구멍 3개소) → **하단 J-후크형 크로메이트 래치 키퍼 브라켓 (실사 203043.png, 0025031.png, 특허 도면 2 부호 1, 좌측 X=+0.148까지 대폭 연장된 180° 곡선 리턴 포켓 & M8 장공 볼트 2세트)**
  - **T자형 단차 흰색 절연 하우징 (`ilWhiteMat`)**:
    - 우측(화면 우측/월드 -X): 흰 칸막이 + **투명 아크릴 전면**. 안에서 단자 나사·ㄷ자 클립·노란전선 종단(흑색 절연 테이프)이 보인다.
    - 백플레이트 검정 원은 그로밋 부품이 아니라 **배선 구멍**. 노란/초록 전선이 구멍에서 나와 아크릴 안 나사로 들어간다.
    - 중앙/좌측(화면 좌측/월드 +X): 수평 고정 블록 (**가로 나란히 M4 십자 볼트 2개**)
    - 좌측 끝단: 동일한 중간 높이(`covCY`)에서 가로로 눕힌 앞·뒤(Z축) 2열 판스프링, 각 끝단 1/2 분할 포크에 나란히 2개씩 원형 접점 리벳 **(총 4개 원형 리벳 접점)**. 하얀색 플라스틱 노즈가 앞·뒤 접점 기부를 균일하게 감싸 동일한 돌출 길이 유지. 노란 전선 및 하단 연결 리드선 정돈.
  - 투명 먼지방지 커버 98×76×26 (전면판 + 4면 측벽, opacity 0.16)
  - ★현대 로고 스티커 만들지 않는다 (사용자 특기 사항)
  - 연동 로프 무간섭: **Y 가 아니라 Z 로 푼다.** 커버는 `z ∈ [-50.5, -24.5]mm`, 로프면은 `z = -8.5mm`
    이므로 로프가 커버 **뒤로 지나간다**. 로프 Y(`caseCY ± ROPE_R` = ±22mm)는 커버 Y(`covCY ± 38mm`)
    안에 들어와도 상관없다. ★커버를 로프면보다 뒤로 옮기면 그때는 Y 여유를 다시 따져야 한다.

- 승장 도어 좌·우 행거 플레이트 어셈블리 — `js/elevator.js` **`buildHangerAssembly(grp, side, cx, g)`**
  (도면 173~174p + 특허 KR200462913Y1 + 실사 211020.png, 001752.png, 001821.png). 좌·우를 두 벌로 쓰지 않고 `side` 부호로 미러한다.
  - 공통: 크로메이트 베이스판(380×160×3.5) + C레일 주행 롤러 2개 + 이탈방지 롤러 1개 + 하단 도어 체결 플랜지/M8 볼트 2세트
  - **월드 +X 패널(화면 왼쪽, `right` 그룹) = 걸쇠측**:
    - **인터록 모듈 골드 크로메이트 구분 (`ilGoldZincMat`)**: 롤러 후면 인터록 전용 장공 베이스판(95×115×3.5) + 상하 2열 종동 롤러(블랙 림+백색 코어)
    - **후크 레버 상단 실사형 코일 스프링**: 8단 토러스 코일 + 중심 조절 볼트 + 하단 스프링 시트 + 상단 누름 와셔 및 M6 조절 더블 너트
    - **45° 사선 챔퍼 갈고리 턱(실사 001821.png, 특허 부호 3)** + **테이퍼 노즈 얇고 넓은 사각 흰색 절연 슈 & 황동 접점판(실사 001821.png, 특허 부호 10)**
    - **좌측 대각선 해정 레버 암 & 비상 삼각키 연동 수직 평철 바(실사 0017521.png, 002051.png)**
    - → 도어 폐문 시 4접점 상부 도킹 및 J-후크 포켓(X=+0.148) 안으로 정확히 낙하·래칭
  - ★**양단 릴레이 풀리 규격**: `ROPE_R = 0.022`, `PUL_R = 0.030`.
    **풀리 홈 반지름이 상·하 가닥 간격(2·ROPE_R = 44mm)을 정하는 단일 원본이다.**
    행거판은 높이 160mm·중심 `caseCY-40mm` 라 윗변이 `caseCY+40mm` 다. `ROPE_R` 이 40mm 면
    상부 가닥이 윗변에 정확히 올라타 판 밖으로 튀어나오고, 플랜지 46mm 는 C레일 상·하 립(±38mm)을 뚫는다.
    22mm 로 줄여야 두 가닥이 판 높이 **안쪽**으로 들어와 판이 앞을 가릴 수 있다(사용자 지시 2026-08-25).
  - ★**연동 로프 Z 는 상·하 분리하지 않는다 — 한 평면 `ROPE_PLANE_Z = HP_PLATE_Z + 0.012`**:
    - 상·하 가닥 모두 **행거판 뒤 12mm**. "Z 를 나눠서 하부만 숨긴다"가 아니라 **판이 앞을 막는다**가 규약이다.
    - 12mm 는 `판 반두께 1.75mm + 풀리 반두께 11mm` 에서 나온다. 8mm 로 잡으면 활짝 열렸을 때
      행거판 바깥변이 풀리를 덮는데도 풀리 앞 플랜지가 판 앞면을 1.25mm 뚫고 나온다.
    - 로프가 보이는 곳은 딱 둘이다: **행거판 사이·바깥의 열린 헤더 구간**, 그리고 **−X 판 타공창 안**.
      좌·우 솔리드 금속면 앞에서는 상·하 어느 가닥도 보이면 안 된다(실사 `2023561.png`).
  - **월드 −X 패널(화면 오른쪽, `left` 그룹) = 수놈 핀측**:
    - ★**행거판 관통 사각 타공창 윈도우**: 폭 184mm × 높이 60mm 실제 개구부 (실사 0130551.png 주황색 영역).
      창 중심은 텐셔너가 아니라 **상·하 가닥 중간(= `caseCY`, 판 로컬 Y +0.040)** 이다.
      그래야 판 뒤로 숨어 온 상부 가닥(`caseCY+22mm`)과 하부 가닥·텐셔너(`caseCY-22mm`)가
      이 구멍 하나로 같이 들여다보인다(위·아래 각 8mm 여유). 창 윗변 위로 판이 10mm 남는다.
    - **실사 일체형 연동 로프 텐셔너 어셈블리 (실사 111111.png, 000500.png, 000712.png, 2023561.png)**:
      - **화면 우측(로컬 -X)**: 긴 전산볼트 고정단(M6) + 지지 탭 양쪽 더블 육각 조절 너트/와셔 2세트 & 압착 슬리브 소켓 (우측 붉은 로프가 상단 풀리 홈으로 진입)
      - **중앙**: 행거판 매립 윈도우 + 블랙 가로 누운 2열 홈 수평 풀리(Ø36, 상단 M8 볼트/너트, 상단홈 ↔ 하단홈 180° 로프 감김)
      - **화면 좌측(로컬 +X)**: 좌측 12회 인장 코일 스프링 가동단 & 스웨이지 슬리브 (하단 풀리 홈에서 나온 푸른 로프가 좌측으로 이동)
    - **보조접점 수놈 플러그 핀 2개 (상·하 뾰족이, L브라켓 결합)** → 커버 안 **ㄷ소켓 2개(파란 접점)** 와 짝
  - ★가동 접점은 `HP_GAP`(26mm) 만큼 **떨어진 상태**로 세운다. 닫힘 위치에서 이미 물린 형상으로 만들지 않는다 (사용자 지시: "떨어진 상태로 결합하지 말고"). 결합 동작은 다음 단계.
  - 걸쇠 갈고리 턱의 로컬 X 는 `HP_TOOTH_X` 하나가 원본이고, 반대편 키퍼가 `cx - HP_TOOTH_X` 로 역산한다. 양쪽에 따로 적지 않는다.
  - 헤더 좌표는 `createHangerCaseAssembly()` 가 `geom` 으로 넘겨준다. 호출부에서 `caseCY`·`lipZ`·`covCY` 등을 다시 계산하지 않는다.
  - 전 층 `hatchDoors[i].left` 및 `hatchDoors[i].right` 그룹에 마운트되어 도어 개폐 FSM과 동기 이동

- 승장 도어 연동(릴레이팅) 로프 — `createHangerCaseAssembly()` + `spinDoorDrive()` (실사 2026-08-18 000500·000627·000712)
  - **원리**: 로프가 안쪽에서 양단 풀리 홈을 타고 180° 감겨 밖으로 나온 뒤, 상·하 가닥이 각각 한쪽
    행거판에 고정된다. 상부 가닥에 월드 +X 판, 하부 가닥에 월드 −X 판이 물리므로 한쪽이 +X 로
    가면 반대쪽은 같은 양만큼 −X 로 끌려간다. 그래서 두 문짝이 **엉키지 않고 항상 대칭 개폐**된다.
  - **화면 오른쪽(월드 −X) = 롤러 고정단**: 로프가 흑색 가로 롤러 위를 타고 그 자리에서 강결(스프링 없음). 연동의 기준점.
  - **화면 왼쪽(월드 +X) = 스프링 고정단**: 로프 끝이 인장 코일 스프링을 거쳐 물린다. 로프 장력은 이 스프링 하나가 잡는다.
  - 형상: 상·하 가닥은 각각 고정단에서 끊긴 **마디 2개씩(총 4개)** + 양단 180° 감김 토러스 2개.
    `spinDoorDrive(h)` 가 도어 행정만 보고 마디 길이와 풀리 회전각(`Δx / ROPE_R`)을 역산한다.
    ★로프 총길이는 항상 `pulRX - pulLX` 로 보존된다 — 검증 시 `upL+upR+2*aHalf` 가 span 과 같은지 본다.
  - ★**Z 레이어**: `ROPE_Z = HP_PLATE_Z + 0.012` — 로프는 행거판 **뒤**다 (2026-08-25 변경, 이전 `-0.024` 폐기).
    이전 값은 로프가 판 맨 앞에 보였는데, 사용자는 "판 앞에서는 연동 로프가 보이면 안 된다"고 지시했다.
    행거판 돌출물(2열 롤러 `z-0.010`, 하단 긴 연동 바 `z-0.014`, −X판 L브라켓 `z-0.012`)은
    모두 판 앞이므로 이제 자동으로 로프보다 앞이다 — 겹침을 따로 따질 필요가 없다.
  - ★**마디 끝점은 고정단이다.** 하부 가닥은 `left ± 0.190`(행거판 좌·우 끝단)이 아니라
    `bL = RB_OFF-0.095` / `bR = RB_OFF+0.082`(타공창 안 텐셔너 결합점)에서 끊는다.
    판 끝단에서 끊으면 `loL`·`loR` 이 판 위에서 **겹쳐 그려져** 금속면 앞에 로프가 드러난다(옛 버그).
  - **+X 솔리드 판의 상부 클램프**: 로프가 판 뒤라서 클램프 블록도 판 뒤다.
    판 앞에는 좌면 패드와 조임 볼트 머리 2개만 나온다 — 실사에서 솔리드 판에 보이는 것도 이것뿐이다.
  - ⚠️ 카 도어 클러치를 다시 올릴 때: 클러치 베인(`z≈-0.030`)은 로프면(`z=-0.0085`)보다 앞이라
    Y 가 겹쳐도 로프를 뚫지 않는다. 베인을 로프면 뒤로 밀 때만 `caseCY ± 22mm` 를 피하면 된다.

- 승장 도어 스프링 클로저 ASSY — `createHangerCaseAssembly()` & `buildHangerAssembly()` (부품설계.pdf 181p)
  - C레일 상단 **+X 끝단** `SPRING CLOSER BRKT` (고정 브라켓, `scbX = hcW/2 - 0.16`)
  - C레일 상부 ㄷ자형 아연도금 보호 덮개 `COVER` — 스프링이 활짝 열림까지 늘어나는 전 구간을 덮되
    전면(승강로 쪽)은 짧은 립만 두어 스프링이 보이게 한다
  - 수평 인장 롱 코일 스프링 `SPRING` — 헬릭스 튜브 1개(`closerCoilGeo`, 단위길이 1m)를
    `mesh.scale.x` 로 늘린다. 늘리면 코일 피치가 같이 벌어져 실제 인장 스프링처럼 보인다.
    닫힘 1.685m → 열림 2.439m (×1.45). 도면 181p 적색 양방향 화살표가 이 신축이다.
  - `left` 그룹(월드 −X 판) 상단 `SPRING HANGER` 체결 러그가 스프링 −X 끝을 끌고 다닌다

아직 올리지 않은 것:

- 승장 도어 패널 본체 (행거판 하단 M8 볼트 플랜지에 체결될 문짝), 리테이너, 라이너

그대로 둔 것:

- 로비 전면벽 개구부, 출입 프레임, 홀버튼, 점자 — `js/environment.js` `buildFrontWallAndLobby()`
- 도어 치수 `S.DOOR_W` 1.50, `S.DOOR_H` 2.10 — `js/config.js`
- 운행 FSM `openDoors()` / `closeDoors()` — 카 도어는 빈 그룹만 움직인다.
  승장 행거판 트윈에는 `spinDoorDrive(h)` 가 `onUpdate`/`onComplete` 로 물려 있다(연동로프 갱신).
- 스티커 PNG `assets/bg/hand.png`, `assets/bg/lean.png` — 삭제 금지

카 에이프런·카 실·체대는 카 재공사로 `js/archive/car.js` / `docs/CAR-REBUILD.md` 로 옮겼다.

`buildCarDoors()`는 운행 코드가 참조하는 빈 `THREE.Group`만 만든다. `buildHatchDoors()`는 실·삼방틀·행거 케이스(양단 브라켓 + C레일 + 연동 풀리 + 연동로프 + 인터록 스위치 박스)와 좌·우 행거 플레이트 어셈블리를 올린다. 인터록 GLB·문짝은 없다.

## 행거 케이스 계약 (에이전트 필수)

사용자 영상(2026-08-17 183216) 음성이 이 구간의 원본이다. 도면 173~174p와 같다. 코드를 손대기 전에 아래를 따른다.

구조 한 줄:

**벽 앙카 L브라켓(양 끝) → C형 행거 케이스(레일).** 행거판·롤러는 문짝과 같이 올린다. 그때 롤러는 C 궤도 안으로 들어간다.

인터록 스위치 박스는 C레일 앞 고정이다. 노란 GLB 판을 공중에 띄우지 않는다.

헤더 전폭은 **고정 상수가 아니라 도어 행정에서 파생**된다(`hcW = 2*(ox + HP_W/2 + 0.075)` ≈ 2.82m).
활짝 열린 행거판이 레일 위에 남아야 하고 연동로프 고정단이 양단 풀리 안쪽에 있어야 하기 때문이다.
헤더가 삼방틀(1.82m)보다 훨씬 넓게 벽 속으로 뻗는 것은 2짝 중앙개폐에서 정상이다. 좁히지 않는다.

| 맞는 것 | 틀린 것 |
|---|---|
| 브라켓이 헤더 **양 끝단**에 있다 (개구부 바깥, 잠 기둥 쪽) | 개구부 한가운데만 보고 브라켓이 없다고 판단 |
| L브라켓이 승강로 벽과 C웹 사이 간극을 채운다 | C레일만 두고 벽과 떨어진 채 떠 있게 둔다 |
| 지금 화면에는 행거판·롤러가 없다 | 문짝 없이 판 2장·롤러를 C레일에 미리 달아 둔다 |
| 주행 롤러는 C 궤도 `z=0` 안에 건다 | 롤러를 레일 옆·앞에 따로 한 쌍 더 둔다 |
| 연동로프는 행거판 **뒤**(`ROPE_Z = HP_PLATE_Z + 0.012`)에서 달리고, 판 앞에서는 안 보인다. 노출은 열린 헤더 구간과 타공창 안뿐 | 로프를 판 **앞**으로 빼서 금속면을 가로지르게 두거나, `z=0`(레일 속)까지 밀어 넣어 열린 구간에서도 안 보이게 만든다 |
| 상·하 가닥 간격은 `2·ROPE_R`(44mm)로 행거판 높이 **안쪽**이다 | `ROPE_R` 을 키워 상부 가닥이 판 윗변 위로 올라타게 둔다 |
| 커버 안에 **ㅓ자 흰 하우징**이 보인다. 중간 볼트 2개, 끝 단자 2+2 가로 일렬, 노란 전선, 돼지발 돌출 | 투명 껍데기만 두고 내부를 빼거나, 단자를 위아래로 세운다 |

하지 말 것:

- `models/gltf/hall_interlock.glb`를 `buildHatchDoors()`에서 다시 로드하지 않는다. 파일은 유지한다.
- 도면 등각투영의 별도 프레임 세트·덮개를 이번에 만들지 않는다.
- 벽 쪽에만 넓은 백플레이트를 두고 C레일과 레일 높이에서 연결하지 않는 고정을 만들지 않는다.
- 브라켓을 트랜섬 높이(레일보다 위)에만 두지 않는다. 브라켓 Y는 C레일 중심(`caseCY`)이다.

검증:

- Live Server 하드 리프레시 후 `?doorcam=1` 로 **헤더 양 끝**을 본다. 가운데만 보면 브라켓·연동 풀리가 안 보인다.
- 노란 인터록 판이 공중에 있으면 잘못된 상태다.
- 문을 열어 본다. 행거판이 레일 끝을 벗어나거나 연동로프 고정단이 풀리를 넘어가면 `hcW` 파생이 깨진 것이다.
- 로프 길이 보존 검증 (콘솔):
  `k=hatchDoors[1].link; k.seg.upL.scale.y + k.seg.upR.scale.y + 2*k.aHalf` 가
  닫힘·열림 어느 위치에서도 `k.pulRX - k.pulLX`(2.59) 와 같아야 한다.
  하부 가닥은 고정단이 비대칭이라 `loL.scale.y + loR.scale.y + (k.bR - k.bL)` 로 본다.
- 로프 가시성 규약 자동 검증: `python -m http.server 8777` 띄우고
  `node tools/verify_relay_rope.mjs 8777`. 닫힘·열림 양쪽에서 12개 항목이 다 PASS 여야 한다.
  (로프면이 판 뒤인지, 두 가닥이 판 높이·타공창 안인지, 마디가 겹치지 않는지, 끝점이 고정단과 맞는지)
- 그림 검증: `node tools/shot_relay_rope.mjs 8777` → `.shot-relay/relay_*.png`.
  ★활짝 열린 상태의 헤더 **양 끝단**을 반드시 본다. 이때 행거판이 풀리를 덮는데,
  로프면이 얕으면 풀리 앞 플랜지만 판을 뚫고 나온다 — 닫힘 화면만 보면 절대 안 잡힌다.

코드 위치: `js/elevator.js` `createHangerCaseAssembly()`, 갱신은 `spinDoorDrive()`.

## 인터록 좌·우 X 규약 (에이전트 필수)

원본(`js/archive/doors.js`)이 정한 월드 X 배치를 그대로 이어간다.

| 부품 | 월드 X | 근거 |
|---|---|---|
| 걸쇠(후크) — 우측 패널에 붙음 | 닫힘 시 ≈ **+0.02** | `idx === 1` 블록, 카 클러치 블레이드 사이 |
| 후크가 무는 키퍼 핀 | ≈ **−0.095** | 후크 암이 `-x` 로 뻗어 감싼다 |
| 인터록 스위치 박스 중심 | ≈ **+0.035** | 원본 돼지발 하우징 +0.042 |
| 암놈 스위치 1개 (걸쇠·빨간 접점) | **+0.058** | ㅓ자 흰 몸체 + 중간 볼트 2 + 끝 단자 2+2 일렬 + 돼지발 |
| 암놈 소켓 2개 (수놈 핀·파란 접점) | **+0.006** | ㄷ자 상·하. 화면 오른쪽 유지 |

★**함정 1 — 좌우 기준**: 인터록은 승강로(−Z)에서만 보이고, 그 시점에서는 **월드 +X 가 화면
왼쪽**에 온다. 사용자가 말하는 "왼쪽/오른쪽"은 이 화면 기준이다. 즉 **화면 왼쪽 = 월드 +X**.
사용자 스크린샷의 좌우와 코드의 X 부호를 그대로 맞추면 틀린다. 확대해서 어느 부품이
화면 어느 쪽에 있는지 먼저 확인할 것.

★**함정 2**: 실사 사진과 좌우가 뒤집혀 보이는 것은 시점 때문이며 모델 오류가 아니다.
사진과 화면을 맞추려고 X를 뒤집으면 후크·클러치 물림 좌표가 전부 어긋난다.

## 원본 코드 위치

파일: `js/archive/doors.js`

| 구간 | 함수·위치 | 부품 |
|---|---|---|
| 스티커 로더 | `getStickerMats()` | hand.png / lean.png 재질 |
| 카문 패널 | `buildCarDoors()` §1 `makeDoor` | PVD 티타늄 패널, 유리, 스티커, 슈 |
| 클러치 | `buildCarDoors()` §2 | 우측문 벌림형 베인 |
| 오퍼레이터 | `buildCarDoors()` §3 | 헤더, 모터, 풀리, 벨트 |
| 카 행거 | `buildCarDoors()` §4 | LM 캐리지, 벨트 클램프 |
| 개폐 연동 | `spinDoorDrive()` | 풀리·연동로프·도어추 |
| 승장문 패널 | `buildHatchDoors()` `makeHatchDoor` | 실버 헤어라인, 스티커 |
| 삼방틀·트랜섬 | 같은 함수 층 루프 | jamb, transom, 층표시 LED |
| 승장 헤더 | `═══ H. 승장 도어 헤더` | 헤더판, 레일, 연동로프 |
| 인터록 | 우측 패널 + 헤더 고정부 | 후크, 롤러, 돼지발 |
| 홀실·토가드 | 층 루프 하단 | hall sill, toe guard |
| 페시아 | `buildHatchDoors()` 루프 뒤 | 층간 수직판 |

재질 원본:

- 카문: `M.pvdTitanium()` 기본 `0xd8cfc2`
- 승장문: `M.silverHairline(0xdfe4ec, 0.28)`
- 에이프런: `M.paint(0x5a3230)` 브라운

스티커 붙임:

- 카문 실내측: 좌문 `lean.png`, 우문 `hand.png`, 패널 안쪽면, 크기 0.132m
- 승장문 로비측: 좌문 `hand.png`, 우문 `lean.png`

## 부품을 다시 올릴 때

1. 이 문서와 `js/archive/doors.js`에서 해당 구간만 읽는다.
2. `js/elevator.js`의 스텁 `buildCarDoors()` 또는 `buildHatchDoors()`에 그 구간만 넣는다.
3. 요청하지 않은 다른 도어 부품을 한꺼번에 복원하지 않는다.
4. 과거 형태를 참고하되, 사용자가 새 자료를 주면 새 자료가 우선이다.
5. 스티커 PNG와 `const S` 도어 치수는 확인 없이 바꾸지 않는다.
6. `index.html`에 아카이브 파일을 연결하지 않는다.

운행 계약(복원 전까지 스텁이 지킨다):

- `carDoorL` / `carDoorR` 과 `hatchDoors[i].left` / `.right`
- `userData.cx`, `userData.ox`
- `spinDoorDrive(h)` — **더 이상 no-op 이 아니다.** 승장 연동로프 마디·양단 풀리 회전·폐문 스프링 길이를
  `h.link` 핸들로 갱신한다. `h.link` 가 없으면 즉시 return 하므로 카 오퍼레이터 복원 시에도 안전하다.
  `ui.js` 의 `openDoors()`/`closeDoors()` 에서 **승장 행거판(`h.right`) 트윈의 `onUpdate`/`onComplete`** 로
  호출한다. ★카도어 트윈에만 물리면 한 프레임 밀려서 로프 끝이 고정단에서 어긋난 채 멈춘다.
- `h.hook`, `h.triKey`, `h.doorWeight`, `h.relPulley` 는 없어도 `ui.js`가 통과한다

## 관련 파일

- 치수: `js/config.js` `S.DOOR_W`, `S.DOOR_H`
- 카 에이프런·카실: `js/archive/car.js` `platformGrp` (카 재공사 원본)
- 로비 개구: `js/environment.js` `buildFrontWallAndLobby()`
- 개폐 FSM: `js/ui.js` `openDoors()`, `closeDoors()`
- 음원: `sound/door_open.wav`, `sound/door_close.wav`, `sound/door_closing_voice.mp3`
