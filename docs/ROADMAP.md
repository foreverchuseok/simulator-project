# ROADMAP — 웹 시뮬레이터 개발 순서

사용자 합의: 2026-09-17. 전체 순서와 인계 상태는 이 문서에서 관리한다. 실제 구현 계약은 각 부품 문서와 현재 코드를 따른다.

## 확정한 방향

- GitHub + Vercel 웹 배포를 유지한다. 직원 약 40~50명, 예상 동시 접속 1~2명의 현장·간단 교육 용도다.
- 직원은 같은 주소로 접속한다. APK 전환과 엔진 교체는 현재 범위에 포함하지 않는다.
- 새 부품보다 부드러운 운행, 관찰하기 쉬운 화면, 모바일 조작을 먼저 완성한다.
- 정밀 부품은 유지하되 전체 운행·부품 관찰·고장 학습에 필요한 상세 수준을 구분한다.

## 개발 순서

| 단계 | 작업 | 완료 기준 | 상태 |
|---|---|---|---|
| 1 | 웹 최적화와 기본 사용 경험 정리. | 기준 기기에서 성능 전후 비교, 기본 운행·모바일 조작·기존 연동 검증 완료. | 현재 단계. 렌더링 개선·간단 배경·관찰 화면 구현과 PC 검증 완료. 다음은 모바일 사용 경험, 실제 모바일 검증 남음. |
| 2 | 추가 부품·기능 약 4~5개. 카도어, 피트 점검운전 등을 포함하며 나머지는 사용자와 구체화. | 항목별 형상·동작 검증 및 성능 기준 유지. | 사용자 요청으로 카 도어 선행 구현·PC 검증 완료. 실제 모바일 확인과 나머지 항목은 대기. |
| 3 | 부품 디자인 보완. | 기존·추가 부품의 형상과 표현 정리, 간극·피벗·동작·성능 확인. | 대기. |
| 4 | 사고 유형별 교육 시나리오 제작. | 시나리오별 재현·단계 설명·복귀와 반복 실행 검증. | 마지막 단계, 대기. |

## 현재 단계 — 웹 최적화

진행 순서는 다음과 같다. 상세 실행 계획은 로컬 `PLAN.md`에 둔다.

1. 기준 측정: 기기·브라우저·해상도·시점·품질 설정을 기록하고 정지·시점 이동·운행·기존 과속 시연을 비교한다. 실제 목표 휴대폰과 성능 기준을 확정한다.
2. 로프 갱신: 운행 중 매번 생성·폐기하는 형상을 재사용하는 방향으로 개선한다. 로프 경로와 카·균형추·시브 연동을 보존한다.
3. 렌더링: 반복 부품과 재질 공유, 고정부품 묶음, 그림자 범위와 해상도 등을 측정 결과에 따라 개선한다.
4. 화면 구성: 기본 배경을 단순화하고 전체 운행·부품 관찰의 상세 수준을 구분한다. 정밀 원본과 가동부 계약을 보존한다.
5. 사용 경험: 모바일 UI·카메라 구도를 정리하고 운행·표시 속도·소리의 일관성을 검토한다.
6. 통합 확인: 실제 모바일과 PC에서 기본 운행 및 기존 안전장치 동작을 확인하고 성능 결과를 남긴다.

## 인계 상태

- 승장 도어·홀버튼 실사화(2026-09-27): 참고 사진(브론즈) 대신 스테인리스 헤어라인으로 `blender/scripts/hall_door_button.py` → `hall_door_panel.glb`·`hall_call_button.glb`. 전 층 도어 본체를 GLB로 교체(스티커는 기존 PNG 그대로), 호출버튼은 헤어라인 판·장애인 표지·원형 버튼+LED 링·검정 채움 촉지 화살표·점자(위 ⠍⠗ / 아래 ⠣⠐⠗), 1층 ▲·최상층 ▼만. 판 중심 +1.02m(편의법 0.8~1.2m). 버튼 LED는 `HallFinish.setLamp()`로 켤 수 있으나 홀 호출 연동은 아직 없다. `verify_car_door`·`verify_hall_manual`·`verify_triangle_key`·`verify_view_modes` 통과, 같은 시점 주 패스 드로우콜 +36. 삼방틀·인디케이터는 이번 범위 밖. 실제 모바일 확인은 남아 있다. 커밋하지 않았다.

- 부품 옆 원형 아이콘(2026-09-27): 사다리 글자 버튼을 없애고 사다리 상단(1.8m) 곁 원형 아이콘으로 바꿨다. 조속기(OVS)·로프브레이크(UCM, 정상/미작동/미설치 선택 소형 패널)·사다리 조작을 고장·점검 메뉴에서 빼고 각 부품 위 44px 유리 아이콘(`js/part-actions.js`)으로 옮겼다. 메뉴에는 운전 모드·승장문·BYPASS·리셋만 남는다. 기존 id(`btn-overspeed`·`btn-ucm`·`ucm-brake`·`pit-ladder-action`) 계약은 유지한다. Codex 작업이 사용량 한도로 끊긴 뒤 Claude Code가 검증을 마무리했다: `verify_part_actions`(PC·모바일), `verify_pit_ladder`(첫 클릭 카메라를 새 아이콘 위치에 맞춤), `verify_ucm`, `verify_overspeed`, `verify_inspection_reset`, `verify_portrait_hud` 통과. 실제 모바일 확인은 남아 있다. 커밋하지 않았다.

- 점검 전체 리셋(2026-09-27): 승장문 패널·고장/점검 메뉴·세로 폰 도크에 공용 리셋을 추가했다. 선택 층과 무관한 전 층 닫힘/삼각키 재잠금, 카문 닫힘, 사다리 접기, BYPASS/비상정지 해제, 브레이크 설정 정상, 자동운전·최근접 층 착상을 처리한다. 고장 시연은 기존 복귀 절차를 재사용하며 중복 입력을 막는다. `verify_inspection_reset.mjs`의 PC/390px 터치·복수 층·사다리 전개 중·점검운전/비상정지·후속 운행 및 UCM/OVS 복귀, 기존 `verify_hall_manual.mjs` 통과. `.shot-inspection-reset/` 화면 확인. 동일 PC Chromium·390×844·DPR 1·동일 시점 비교 중앙값 16.7ms, p95 33.4ms, 호출 1,435회 유지. 실제 모바일 확인은 남아 있다. 커밋·푸시하지 않았다.

- 세이프티슈 전선 상부 인출 정정(2026-09-27): 사용자 표시 0015461·002938에 따라 잘못 유지했던 하부 八자 리드·실 아래 여유선·측면 상승 복귀선을 삭제했다. 선단 상부 → 양옆 → 헤더 뒤 가동 여유선 → 카탑 박스 경로다. `verify_car_lock_detail.mjs`의 101개 자세 연결·헤더 뒤 위치·버퍼 재사용 및 `verify_car_wiring.mjs` 통과, 닫힘/열림 화면 확인. 실제 모바일 확인은 남아 있다. 상세는 `docs/CAR-DOOR.md`. 커밋·푸시하지 않았다.

- 카 실내 손잡이(2026-09-27): 사용자 표시 사진과 치수에 따라 후면 손잡이를 중심 높이 900→850mm로 낮추고 양측까지 Ø35mm 연속 관을 설치했다. 둥근 코너·벽 반환 끝단·금속 이음 기둥 취부를 적용하며 출입구는 비운다. 사용자 선택 1번으로 측면 조작반 버튼 중심은 1050mm로 올려 손잡이와 72.5mm 수직 간극을 확보했다. `verify_car_handrails.mjs`의 수치·층별 추종·390px 터치 운행/도어·렌더 검증 통과. 동일 PC Chromium·390×844·DPR 1·실내 동일 시점에서 손잡이 추가 전후 중앙값 16.7ms, 호출 237회 유지(고정 지지물 배칭). 실제 모바일 확인은 남음. 상세는 `docs/CAR-REBUILD.md`.

- 모바일 세로 HUD(2026-09-26, 232532 음성 녹화 반영): ≤600px 세로 폰은 우측 세로 운행바(4F→1F·열림·닫힘·STOP), 좌측 고장 단축 도크(MODE·OVS·UCM·BRK·KEY·LAD·BYP)로 배치한다. MODE/BRK/BYP만 작은 설정창을 펼치며, 좌하단 눈 버튼으로 도구를 숨기고 복원한다. 상태·고장 복귀는 유지한다. 기존 DOM 버튼을 이동해 상태/이벤트를 공유하고 가로·PC에서는 원위치로 복원한다. 초기/전체 보기 구도는 세로 폰에서 기계실부터 피트까지 보정한다. `tools/verify_portrait_hud.mjs`의 320·360·390·412px 터치/화면 회전/설정/승장문/비상정지/층 이동/사다리/개문발차·RST 검증과 `verify_view_modes`, `verify_overspeed --mobile` 통과. 동일 PC Chromium·390×844·DPR 1·동일 카메라에서 프레임 중앙값 16.7→16.7ms, p95 33.4→33.4ms, 호출 4,129회 유지. 실제 휴대폰의 변경 후 조작·성능 확인은 남아 있으며 커밋·푸시하지 않았다.

- 과속 시연 순차 연출(2026-09-26): 사용자 요청으로 화면 분할을 제거했다. 3층 이상 승강로 측면에서 주로프 파단·카 낙하→기계실 진자/쐐기/캐치슈→하부 링크→레일 파지를 한 화면씩 보여준다. 조속기 로프는 유지하며 레일 파지에 짧은 불꽃·충격음·진동을 동기화했다. RST에서 파단 표시와 카 외장을 복원한다. PC·390px 터치 에뮬레이션의 단계/로프/링크/복귀·구출 검증 통과. 같은 PC 두 확대 시점 전후 프레임 중앙값 16.7ms 유지. 실제 모바일·스피커 확인은 남아 있다. 상세는 `docs/GOVERNOR-DESIGN.md`.

- 조속기 스위치 타격 정렬(2026-09-26, 175622·1756221): 진자 뭉치와 스위치 암의 앞뒤 위치를 맞추고 정상 회전 간극을 확보했다. 내려오는 캐치 브라켓은 가로축 원형 끝을 누르며, 첫 전기 차단 이후 접촉 궤적에 맞춰 추가 회전한다. Python·GLB·트립 애니메이션 동기화 완료. 실제 메시 접촉·과속/RST/구출 검증 통과, 오류 0건. 같은 PC 확대 시점 프레임 중앙값 전후 16.7ms·호출 494회 유지. 상세는 `docs/GOVERNOR-DESIGN.md`. 실제 모바일 확인은 남아 있다.

- 피트 사다리 정면 전개(2026-09-26, 112107·1121071 표시 사진 반영): 상단 고정 스위치는 승장 +1.045m, 흰 클립은 +1.00m로 옮겼다. 사다리는 접혔을 때 발이 40mm 떠 있고, +X 145mm 정면으로 나오며 40mm 내려와 양발이 피트 바닥에 닿는다. Z 이동은 0이다. 펼침 상단은 승장 +1.10m, 접힘 상단은 +1.14m다. 링크는 위아래 양쪽 레일 끝의 4개이며 Z축 회전으로 전개한다. 벽 축과 레일 축의 연결을 전 구간 유지한다. 수평 위치를 유지하므로 승장 기준면부터 중심은 접힘·펼침 모두 490mm다. 기존 사다리 옆 버튼·스위치 운행 차단을 유지했다.
- 사다리 스위치: 사용자 실사 AVIF의 검은 몸체·장착 구멍 2개·단자 나사 3개·황동 나사산·육각 너트·은색 누름봉을 `blender/scripts/pit_ladder_switch.py` → `models/gltf/pit_ladder_switch.glb`로 제작했다. 형상 치수는 사진 기반이며 실측 치수는 아니다. `PIT_LADDER_SWITCH`의 눌린 끝 위치·6mm 스트로크를 Python이 읽는다. `SwitchBody`와 `SwitchPlunger` 노드를 분리하고, 접힌 판이 누르면 운행 회로 연결, 펼쳐 놓으면 개로된다. 접점 A/B 명칭 대신 이 동작을 코드 계약으로 쓴다. 펼치기 시작부터 접힘 클립이 다시 고정될 때까지 자동·점검·구출 운행이 차단된다.
- 사다리 최신 검증: `tools/verify_pit_ladder.mjs`에서 짧은 링크(≤350mm)·양발 부상(링크 파생 약 155mm)→바닥 착지, 스냅 클립 고정·벌어짐·복귀, Z 이동 0, 펼침 높이, 링크 연결, 상단 스위치 누름판 접촉, 카 간극·운행 차단과 복구·390px 터치를 검증한다. `temporary/pit-ladder-straight.json`과 `.shot-pit-ladder/`에 결과를 기록한다. 2026-09-26 재실행에서 오류 0건. 실제 모바일 조작 확인은 남아 있다. 커밋·푸시하지 않았다.
- 사다리 표시 전후 성능: 같은 PC·Chromium·1280×850·DPR 1에서 사다리를 숨긴 상태와 표시한 상태를 비교했다. 정지 중앙값 24.6→24.5ms, p95 27.3→26.2ms, 호출 4,084→4,129회, 삼각형 1,168,146→1,184,964개다. 모바일 실기기 성능을 의미하지 않는다.

- 승강로 조명·피트 정지 스위치(2026-09-26): 조명 스위치는 르그랑 슈퍼와이드 1구 디자인으로 최상·최하층 승장 +1m에 둔다. 피트 정지는 노란 비상정지+LIGHT only 콘센트 박스로, 박스 상단이 +0.385m다. 좌측벽 전면 모서리에 있으며 카 도어 하부 케이블 띠를 피한다. 상세는 `docs/TRAVEL-CABLE-TERMINAL.md`. `verify_travel_cable.mjs` 54/54 통과.

- 안내 음성·음량 후속(2026-09-26): 사용자 청취 피드백에 따라 효과음 공용 게인을 0.7→1.2로 올렸다. 안내 7개는 `ko-KR-SunHiNeural`, 속도 -5%로 생성한 `sound/announce_*.mp3`를 우선 재생한다(제조사 녹음 아님). FFmpeg loudnorm -16 LUFS / true peak -1.5 dBTP / mono 24 kHz·48 kbps로 음량을 정리했으며 브라우저 안내 볼륨은 1이다. 파일 실패 시 기존 기기 TTS로 대체하고 새 안내가 시작되면 이전 안내를 중단한다. 실제 모바일 음질·성능 확인은 남아 있다.

- 소리 정리(2026-09-26): `js/ui.js`의 기존 한국어 TTS 변경을 이어서 적용했다. 운행음의 고조파·고주파를 줄이고, 도어 WAV·도착 WAV 재생을 공용 Web Audio의 짧은 도어음·2음 차임으로 교체했다. 도어음 시간은 `js/car-door.js`의 열림 .24+1.15초·닫힘 .95+.24초와 동기화하며 도어 타임라인 변경 시 함께 갱신한다. 안내 중 기계음 감쇠, 한국어 음성 선택 우선순위, 오래된 음성 로딩 요청 무시, 종료 노드 연결 해제와 동일 속도 갱신 생략을 적용했다. 오프라인 오디오 렌더의 동시 재생 피크 0.25 미만·비정상 샘플 0, 안내 선택 검사·구문 검사·`verify_car_door.mjs`의 운행/도어/터치 에뮬레이션 통과. 실제 스피커 청취·모바일 음성/성능·동일 조건 성능 비교는 미검증이며 한국어 안내 음질은 설치 음성에 의존한다. 커밋·푸시하지 않았다.

- 배포(2026-09-26 2차): 사용자 요청으로 개문발차 연출 보강(승객이 올라가는 카 바닥을 덮친 뒤 기계실 전환, 기계실 옆·뒤 사선 카메라, 넘어짐 굽힘각 표)과 모바일 컴팩트 모드(폰에서 설명 제거·약자 라벨·2열 타일, `100dvh`로 하단 운행바 가림 해소)를 main에 푸시해 Vercel 자동 배포했다. 배포 전에 `verify_ucm`(3모드)·`view_modes`·`hall_manual`·`car_underbody`·`pit_ladder`를 통과했다.
- 배포(2026-09-26): 사용자 요청(모바일 확인용)으로 누적 미커밋 작업을 한 커밋으로 main에 푸시해 Vercel 자동 배포했다. 포함 작업은 피트 사다리(짧은 링크·스냅 클립), S3-B1370 리미트 스위치 GLB·가로 브라켓, 개문발차(UCM) 로프브레이크 시연, HUD 재설계(카메라 메뉴 제거·하단 운행바·상태 카드·고장·점검/설정 시트), 안내 음성 mp3다. 배포 전에 `verify_view_modes`·`ucm`·`overspeed`·`hall_manual`·`pit_ladder`·`car_door`·`car_underbody`·`sky_island`·`traction_machine`·`travel_cable`를 통과했다. 실제 모바일 조작 확인은 사용자가 할 예정이다.
- 배포(2026-09-24): 사용자 요청으로 누적 미커밋 작업(균형추·피트 스크린·바빗 히치·로프브레이크·권상기·이중브레이크·엔코더·마스코트 등)을 한 커밋으로 main에 푸시해 Vercel 자동 배포했다. 배포 전 `verify_traction_machine`·`machine_room_safety`·`wire_rope`·`babbitt_hitch`·`overhead`·`overspeed --shadows`·`counterweight`·`pit_screen`·`cwt_buffer_gap`·`car_door`·`travel_cable`·`governor_design`·`view_modes`·`shadow_cache`를 통과했다. 이전 배포 삭제는 하지 않았다(“5개 빼고 삭제” 요청 시에만).
- 기계실 마스코트 「승강곰」(2026-09-24): 오리지널 캐릭터(외부 IP 없음). `js/mascot.js`, 카메라 메뉴 「캐릭터」 버튼으로 표시/숨김. 키 약 0.85m, 손 흔들기·까딱·깜빡임. 그림자 캐시를 깨지 않도록 castShadow 끔 + 발밑 고정 원판.

- 권상기 이중브레이크·시브·가드·배선(2026-09-24): 사용자가 준 `티케이 구동기 설치 매뉴얼.pdf`(TKE 이중브레이크, 검사기준 12.4.2.1 기계 부품 2세트)를 반영해 단일 전자석·1스프링을 TM30B식 이중브레이크로 교체했다. 도금 2솔레노이드 몸체, 노란 코일 단자박스, 좌우 독립 스프링 116mm(11kW 표), 눈금자, MS1/MS2, 적색 표기, 암↔고정너트 4mm로 구성했다. 로프브레이크(상승과속·개문출발 방지)는 기존 그대로다. 시브는 사용자 참고 사진(스크린샷 103301)처럼 노란 주물·곡선 스포크·검은 홈 림으로 바꿨고, 현수도르래도 같은 설계를 작게 적용한 `deflector_sheave.glb`로 교체했다. 가드는 사용자 표시(스크린샷 1033381)대로 앞은 내려가는 로프 스커트, 뒤는 로프 결을 따라 로프브레이크 앞까지 늘렸다. 이중브레이크·모터·엔코더 전선관은 새 바닥 덕트 분기로 제어반에 들어간다. 계약은 [TRACTION-MACHINE.md](TRACTION-MACHINE.md)에 있다.
- 검증: `verify_traction_machine.mjs`(가드↔로프 43.9mm, 터널↔로프브레이크 70mm, 현수 홈 5개 일치, 전선관 3가닥 연결), `verify_machine_room_safety`·`verify_wire_rope`·`verify_babbitt_hitch`·`verify_overhead`·`verify_overspeed --shadows` 통과, 오류 0건. 성능은 같은 PC·조건에서 호출 4,042회·삼각형 1,118,498개다. 이번 측정 세션에서는 정지 33.3ms·운행 33.4~50ms가 나왔다. 같은 시점에 다시 잰 변경 전 사본도 정지 33.3ms여서 PC 상태에 따른 편차로 본다(`.shot-rope-performance/traction-dual-after*.json`). 실제 모바일과 사용자 화면 비교는 남아 있으며 커밋하지 않았다.

- 권상기 Blender 재제작(2026-09-24): 사용자 사진(`Pictures/Screenshots/권상기`)의 동양엘리베이터 전시용 절개 권상기(2005)와 웜·휠 근접 사진을 참고했다. JS 프리미티브 + 1.5배 스케일이던 기어드 권상기를 `traction_machine.py` → `traction_machine.glb`(회색 도장)로 교체했다. 구성은 휠 위 2줄 웜 · 청동 50치 목 휠(25:1) · 컵형 브레이크 드럼과 양 암·전자석·스프링 · 전동기(냉각핀·명판·엔코더) · 5홈 스포크 시브 · 노란 가드 · 기어 케이스(점검창·유면계·드레인·주유구)다. 카메라 메뉴에 「권상기」「권상기 내부」 버튼을 추가했다. 내부 보기는 케이스 1/4을 빼내 빨간 절단면, 웜·휠 이물림, 오일 레벨을 보여 준다. 운행 시 웜이 시브와 25:1로 돌고, 브레이크 암이 개방·체결된다. 기존 시브가 받침대 상면을 45mm 파고들던 간섭도 없앴다. 로프 경로·`mainY/mainZ/mainR(0.33)`·로프브레이크·현수도르래 위치는 그대로다. 계약은 [TRACTION-MACHINE.md](TRACTION-MACHINE.md)에 있다.
- 권상기 검증: `verify_traction_machine.mjs` 통과(로프 5홈 바닥 0.3235 일치, 휠 12위상 × 3면 웜/휠 간섭 0mm, 25:1 연동, 브레이크 개방, 절개 토글, 받침대·가드·암·로프브레이크 간극). `verify_machine_room_safety`(접선 오차 3.6e-15)·`verify_wire_rope`·`verify_babbitt_hitch`·`verify_overhead`·`verify_overspeed --shadows` 통과, 브라우저 오류 0건. PC·390px 폭 버튼과 절개 시점을 확인했다. 성능은 스크래치 사본으로 변경 전 상태를 재구성해 같은 PC·1280×850·DPR 1·그림자 켬·각 3회로 비교했다. 정지·회전 16.7ms, 운행 33.3ms(p95 33.4ms)로 전후가 같다. 기본 호출은 4,207→4,027회, 삼각형은 999,287→1,081,786개다. 기록은 `.shot-rope-performance/traction-before.json`, `traction-after.json`에 있다. 실제 모바일 조작·성능과 사용자 화면 비교는 남아 있으며 커밋하지 않았다.
- 참고: `rope_brake.py`는 옛 `const rx = … + i * …` 정규식으로 로프 X를 읽는다. `ROPE_GROOVE_X` 배열로 바뀐 뒤로는 재실행에 실패한다(GLB는 영향 없음).

- 균형추 Blender 재제작·오일통 교체(2026-09-24): 사용자 사진(20260825_130032·IMG_3081·스크린샷 005751·005848·001412·010024·010249)을 반영했다. `counterweight.py` → `counterweight.glb`로 균형추를 교체했다. 구성은 채널 기둥, 상부 빔 120mm·하부 빔 160mm, 모서리 거싯 볼트, 검은 주물 웨이트 21단(좌·우 양쪽 앞·뒤 막힌 손잡이 홈·가운데 「30」 양각), 좌측 기둥 전면 마커 번호 1~21(위부터), 상·하 검은 가이드슈(아연 브라켓·8K 레일 날 U홈), 상부 슈 위 상자형 오일통이다. 기존 기둥이 8K 레일과 겹치던 문제도 해소했다(기둥 바깥면 ↔ 날끝 22mm). 카 상부 가이드슈 오일통은 원통 컵에서 같은 상자형(`build_box_oiler()`)으로 바꿨다. 사진처럼 균형추 히치는 스프링 없이 빔 밑 와셔·더블너트·분할핀으로 두었다(`springs:false`).
- 검증: `verify_counterweight.mjs`(레일 날 관통 0, 프레임↔날끝 17mm, 슈 홈 반폭 5.0mm, 펠트 날끝 접촉, 카 오일통 간섭 0, 카↔균형추 86mm, 근접 스크린샷 `.shot-counterweight/`), `verify_babbitt_hitch`·`verify_car_guide_shoe`(오일통 재질 접두어 허용으로 수정)·`verify_pit_screen`·`verify_cwt_buffer_gap` 통과, 브라우저 오류 0건. 균형추 GLB 메시 17개. 실제 모바일 성능과 사용자 화면 비교는 남아 있으며 커밋하지 않았다.
- 주로프 5본 바빗 히치(2026-09-24): MR 로프체결도 「TOP BEAM 로프히치 홀」 5본·후락 칸을 적용했다. 7홀 육각 판에서 좌상·우상·중앙·좌하·우하 5홀만 쓰고 좌·우 중단은 빈 홀로 둔다. 홈 X 일렬의 로프 1~5는 좌하·좌상·중앙·우하·우상 홀로 간다(도면 위 = 후면). 조립은 테이퍼 소켓(클레비스 핀·분할핀) → 로드 → 히치판·빔 관통 → 빔 아래 시트판·흑색 스프링·더블너트 순이다. 카 크로스헤드와 균형추 상부가 `js/elevator.js`의 `buildBabbittHitch()` 하나를 쓰고, 홀 좌표 원본은 `ROPE_HITCH_XZ`다. 균형추 스프링 공간 때문에 웨이트 블록을 20→18단으로 줄였다. 로프는 5본 공유 정적 시브 감김 호와 로프별 카·균형추 직선 하강부로 나누었으며, 운행 중에는 하강부 위치·길이·UV만 갱신하고 꼬임 무늬는 텍스처 offset으로 카와 함께 흐른다. 참고 영상은 `temporary/rope-wire/`의 로프 교체 공사 영상이다.
- 바빗 히치 검증: `verify_babbitt_hitch.mjs`(로프 끝↔소켓·시브 접점 오차 1e-15 수준, 로드↔크로스헤드 웹 1.4mm 여유, 스프링 영역 카 부품 간섭 0, 균형추 꼬리↔블록 25mm, 1↔4층 운행 추종, 근접 스크린샷), 새 구조로 고친 `verify_wire_rope.mjs`(25개 위치·정적 호 버퍼 불변·정지 시 UV 갱신 생략), `verify_cwt_buffer_gap.mjs`, `verify_shadow_cache.mjs` 통과, 브라우저 오류 0건. `verify_rope_performance.mjs babbitt`에서 로프 갱신 중앙값 0.7ms·TubeGeometry 생성 0회, 기본 호출 4,217회, 프레임 중앙값 정지 33.4ms·운행 50ms(이전 기록과 같음). 히치는 재질별로 묶어 조립체당 메시 7개다. 실제 모바일 조작·성능과 사용자 화면 비교는 남아 있으며 커밋하지 않았다.
- 조속기 디자인 보완(2026-09-21): `device_china.mp4` 31~36초와 사용자 설명 영상 150806을 확인하고 기존 기본 구조를 유지했다. Blender 원본에서 얇은 스위치 케이스·절곡 커버·액추에이터, 쐐기 곡선과 체결부, 원통 진자와 좁은 연결부, 캐치 레버 윤곽·모서리를 보완하고 GLB를 재내보냈다. 조속기 금속에만 반사 환경을 적용했으며 도르래 감김 로프의 UV 방향·6연선 요철을 수정했다. [GOVERNOR-DESIGN.md](GOVERNOR-DESIGN.md)에 구현 계약을 기록했다.
- 조속기 검증: `verify_governor_design.mjs`의 캐시 비활성화 전후 비교에서 주요 피벗·타격점·타격 위상·트립 각도 유지와 오류 0건을 확인했다. `verify_overspeed.mjs --shadows --detail-view`의 전기 차단·쐐기·로프 파지·RST·구출 운전 통과, 전면·사선·스위치 확대와 작동 장면을 확인했다. 실제 모바일 조작·성능과 사용자 화면 비교는 남아 있다. 커밋·푸시·배포하지 않았다.
- 조속기 성능: 같은 PC·1280×850·DPR 1·기본 시점·그림자 켬·각 3회에서 초기 전후 측정은 정지/회전 중앙값 16.7→33.3ms, 운행 33.3→50ms로 달랐다. 추가 반사 환경을 켜고 끈 비교는 정지 중앙값 33.3ms로 같았다. 원래 GLB와 원래 로프 감김 형상·반사 설정을 브라우저 응답으로 복원해 전/후/전/후 순서로 다시 비교한 결과 모두 정지 33.3ms·운행 50ms로 같아 이번 변경에 의한 저하로 확인되지는 않았다. 기본 호출 4,222→4,218회, 삼각형 932,609→947,525개이며 로프 재생성 0회·버퍼 재사용·오류 0건이다. 기록은 `.shot-rope-performance/governor-refinement-before.json`, `governor-refinement-after.json`, `.shot-governor-design/paired-performance.json`이다. 실제 모바일 결과를 의미하지 않는다.

- 로프브레이크 Blender 고급화(2026-09-21): 사진 075811/075803/075936/080035를 반영해 `rope_brake.py` → `rope_brake.glb`로 본체를 교체했다. 주물 금속 표면·모서리 가공·상부 로프 홈 5개·하부 평면 턱·Safe-Tech 양각·양쪽 수동 손잡이와 고정 해제 레버·덮개 경고 표기를 추가했다. JS 받침·본체 위치와 피치·기존 배선을 유지하고 공용 장착 계약을 GLB 로드 시 검사한다. Blender 내보내기, Live Server 캐시 비활성화 후 15개 지점의 실제 홈/접촉면·받침·배선 검사와 본체 상하부 확대 화면 확인을 통과했다. 실제 수동 해제 애니메이션을 추가한 것은 아니다. 실제 모바일 조작·성능은 남아 있다.
- Blender 교체 전후 성능: 동일 PC·1280×850·DPR 1·기본 시점·그림자 켬·각 3회. 실행 중앙값의 중앙값은 정지·회전 전후 16.7ms, 운행 전후 33.3ms, p95 약 33.4ms다. 본체를 부품·재질별로 묶어 기본 호출은 4,240→4,222회, 삼각형은 912,286→932,609개다. 로프 버퍼 재사용·재생성 0회·브라우저 오류 0건이다. 원본은 `.shot-rope-performance/brake-blender-before.json`, `brake-blender-after.json`이다. 이후 하부 판 두께·양각 접촉면을 보정하고 같은 형상 검증을 다시 통과했다. 실제 모바일 검증과 사용자 화면 비교는 남아 있으며 커밋·푸시·배포하지 않았다.

- 기계실 로프브레이크·배선(2026-09-21): 사진 071649/072232와 로컬 로프브레이크 교체 영상을 참고해 노란 절곡 덮개·회색 턱·가이드 봉·렌치·도금 측판을 보완했다. 떠 있던 얇은 기둥을 체대 양쪽 채널에 볼트로 고정하는 가로 받침 2개·받침 슈·발판·각도 고정 측판으로 교체했다. 도르래 위치와 기존 로프 접선·브레이크 위치는 유지했다. 조속기 스위치와 로프브레이크 전선은 바닥까지 노출하고 높이 22mm의 경사 덕트로 제어반 하단에 연결했다. 구현은 `js/machine-room-safety.js`, 연결은 `js/environment.js`·`index.html`이며 [MACHINE-ROOM-SAFETY.md](MACHINE-ROOM-SAFETY.md)에 계약을 기록했다.
- 기계실 검증: 조속기 Python 원본에 전선 연결점 메타데이터만 추가하고 GLB 재내보내기 성공. Live Server 캐시 비활성화 후 `verify_machine_room_safety.mjs`에서 접선 오차 약 3.6e-15m, 받침 접촉, 전선의 가로 받침 관통 없음과 브라우저 오류 0건을 확인했다. 기계실 전체·브레이크 양쪽·조속기 배선 화면을 확인했고 `verify_overspeed.mjs --shadows`의 트립·복귀·구출 운전도 통과했다. 실제 모바일 조작·성능과 사용자 화면 비교는 남아 있다. 커밋·푸시·배포하지 않았다.
- 기계실 전후 성능: 동일 PC / 1280×850 / DPR 1 / 기본 시점·그림자 켬 / 각 3회에서 실행 중앙값의 중앙값은 정지·회전 전후 16.7ms, 운행 전후 33.3ms였다. 변경 후 정지 1회 33.3ms, 운행 p95 1회 50ms의 편차가 있었고 나머지 p95는 약 33.4ms다. 기본 호출 4,179→4,240회, 삼각형 907,742→912,286개, 로프 형상 재생성 0회·버퍼 재사용·오류 0건이다. 원본은 `.shot-rope-performance/machine-safety-before.json`, `machine-safety-after.json`이다. 실제 모바일 성능을 검증한 결과는 아니다.

- 이동케이블 현장 형태·방향 수정(2026-09-21): 사진 104115/104117, 영상 104129, 참고 스크린샷 001043의 이동케이블만 반영했다. 자유 U를 카 하부 안쪽으로 돌아오는 XY 평면으로 바꾸고, 카 하부 둥근 지지부·실제 보강 채널 취부·승강로 중간 고정 브라켓·클램프·회색 외피 세로 결을 구현했다. `index.html`의 `TC_*`와 `js/elevator.js`를 수정했다. 자세한 계약은 [TRAVEL-CABLE-TERMINAL.md](TRAVEL-CABLE-TERMINAL.md) 최상단이다.
- 이동케이블 검증: `verify_travel_cable.mjs` 54/54, `verify_travel_cable_shape.mjs`, Live Server `verify_travel_cable_clearance.mjs` 통과. 최하/최상 오버런 포함 61개 자세에서 자유 케이블과 피트·사다리·레일·카 일반 메시 삼각형의 간섭 0건, 최하층 바닥 여유 약 296.5mm·하부 오버런 최소 약 121.7mm, 길이 유지·지지부 접점·버퍼 재사용 통과, 오류 0건이다. 피트·카 하부·중간 브라켓 화면을 확인했다. 실제 모바일 조작·성능과 사용자 화면 비교는 남아 있다. 커밋·푸시·배포하지 않았다.

- 균형추 완충기 여유 수정(2026-09-21): 사용자 사진 000038의 최상층 착상 시 접촉을 수정했다. 기존 균형추 하단 Y0+350mm와 저속 완충기 상단 Y0+350mm가 같아 간격이 0이었다. `js/environment.js`의 `BUFFER_DIM`과 `counterweightBottomHeight()`를 공유하여 가장 높은 유압 완충기 상단 +160mm를 균형추 최하단으로 사용한다. `js/elevator.js`에서 균형추 초기 위치를 625mm 올리고 기존 로프 끝점 추종을 유지한다. 균형추 치수와 FSM은 그대로이며 속도 변경 시 균형추는 순간 이동하지 않는다.
- 여유 검증: Live Server에서 최상층 운행 시 저속 완충기 625mm, 90m/min 유압 완충기 160mm, 최하층 균형추 상단의 천장 여유 3.4m를 확인했다. `verify_cwt_buffer_gap.mjs`의 왕복·로프 끝점 오차 0·속도 변경 시 위치 유지·오류 0건 통과. `verify_travel_cable.mjs`는 예전 초기 높이 12.5m 하드코딩을 실제 초기 위치로 바꾸고 54/54 통과했다. 이동케이블 행거와 길이는 기존 계산에 따라 함께 파생된다. 실제 모바일은 미검증이며 커밋·푸시·배포하지 않았다.

- 피트 스크린(2026-09-20): 사용자 요청으로 `부품설계.pdf` 254~255쪽을 반영한 노란색 얇은 철제 보호판을 후면 균형추 앞에 추가했다. 사진 233834는 철판 형태만 참고했다. `blender/scripts/pit_screen.py` → `models/gltf/pit_screen.glb`, `js/environment.js`의 `buildPitScreen()`으로 연결한다. 1.5mm 철판 두 장·18mm 절곡·4단 브라켓·M6x10 체결이며 하단은 Y0+200mm(바닥 마감 위 180mm)다. 상세는 [PIT-SCREEN.md](PIT-SCREEN.md)다.
- 피트 스크린 PC 검증: Blender 내보내기 성공, Live Server 캐시 비활성화 후 `verify_pit_screen.mjs` 통과. 카 하부 형상 정점 기준 여유 약 65.25mm, 균형추 프레임·슈의 전 이동 범위 간섭 없음, 최상층 실제 운행 중 스크린 고정 유지, 브라우저 오류 0건이다. `.shot-pit-screen/` 정면·후면·철판 확대·균형추 최하단 화면을 확인했다. 실제 모바일 조작·성능은 미검증이며 사용자 화면 비교가 남아 있다. 커밋·푸시·배포하지 않았다.
- 피트 스크린 전후 성능: 동일 PC / 1280×850 / DPR 1 / 그림자 켬 / 기본 시점에서 정지·회전·운행 각 3회. 실행 중앙값의 중앙값은 정지·회전 전후 16.7ms, 운행 전후 33.3ms, p95는 모두 약 33.4ms다. 추가 후 정지 1회는 33.2ms로 편차가 있었다. 기본 호출 4,145→4,149회, 삼각형 892,970→895,874개다. 왕복 운행·도어 개폐, 로프 버퍼 재사용·TubeGeometry 재생성 0회·오류 0건을 확인했다. 원본은 `.shot-rope-performance/pit-screen-before.json`, `pit-screen-after.json`이며 실제 모바일 결과는 아니다.

- 상부 카도어락 현재본 PC 성능(2026-09-20): 기존 1280×850 / DPR 1 / 그림자 켬 / 기본 시점 조건으로 정지·회전·운행 각 3회 측정했다. 정지·회전 중앙값은 모두 33.3ms, 운행 중앙값은 33.4/33.4/49.9ms이고 p95는 모두 50.0ms다. 기본 호출 4,145회·삼각형 892,970개, 로프 버퍼 재사용·TubeGeometry 재생성 0회·오류 0건이다. `.shot-rope-performance/upper-lock-resume.json`에 기록했다. 같은 세션에서 변경 전을 측정하지 않았으므로 전후 성능 개선/저하를 단정하지 않는다. 실제 모바일 결과는 아니다.

- 상부 카도어락 재개 검증(2026-09-20): 마지막 저장된 165414 영상 기준 `lockSpec`/`buildUpperLock()`의 후크·관통 걸쇠·잠금 접점 형상을 유지하고 최신 코드로 검증을 마쳤다. `verify_car_lock_detail.mjs`의 101자세 슬롯/후크 간섭·접점 분리·상자 여유와 기존 슈/배선 검사를 통과했다. 잠금 이빨 삽입 16mm, 해정 여유 31.45mm, 접점 분리 69.43mm, 상자 최소 여유 3.58mm다. Live Server 캐시 비활성화 후 `verify_car_door.mjs`의 개폐·운행·비상정지/복귀·터치 에뮬레이션도 통과했고 오류는 0건이다. `.shot-car-lock/` 정면·잠금·해정 화면을 확인했다. 최초 샌드박스 실행의 라이브러리 로드 실패는 네트워크 허용 실행으로 해소했다. 다음은 사용자 화면 비교와 실제 모바일 조작·성능 확인이며, 후속 부품은 사용자 지시 후 진행한다. 커밋·푸시·배포하지 않았다.

- 카도어락·도어슈·하부 배선 보완(2026-09-20): 현장 사진 112454/112457/112459, 영상 112508, 표시 이미지 1627381/1628291을 반영했다. `js/car-door.js`에서 클러치/후크/링크의 진한 금속 대비, 베인 절곡면·피벗 부시, 잠금 접점 커버와 양쪽 게이트 접점을 보완했다. 세이프티슈 선은 얇은 하부 八자 경로와 카탑 박스 인입으로 바꾸고, 해제 와이어는 레버 끝에서 문 하단까지 연결했다. 도어슈는 취부판·금속 목·검은 슈로 구분하고 기존 앞쪽 실 홈에 정렬했다. `js/car-panels.js`의 홈 중심은 `CarDoor.dimensions().guideZ`를 공유한다. `const S`, 기존 운행 FSM과 승장 결합은 유지한다. 상세는 [CAR-DOOR.md](CAR-DOOR.md)의 해당 계약이다.
- 보완 검증: Live Server 캐시 비활성화 후 `verify_car_lock_detail.mjs` 101자세의 슈 측면 1.5mm/바닥 2mm 여유, 전 행정 실 지지, 레버 와이어 연결, 하부 여유선 실 간섭 부재·버퍼 재사용을 통과했다. `verify_car_door.mjs` 정상 운행·개폐·잠금·비상정지·복귀·터치 에뮬레이션 및 `verify_car_wiring.mjs` 외판/천장/플랫폼 간섭·카탑 인입 검사를 통과했다. 정면·슈 확대·잠금/해정·개방 PNG는 `.shot-car-lock/`에서 확인했다. 오류 0건이며 실제 모바일 조작·성능은 미검증이다. 다음은 사용자 화면 비교와 실제 모바일 확인이다. 커밋·푸시·배포하지 않았다.
- 보완 전후 PC 성능: 동일 Radeon Graphics / Chrome 149 / 1280×850 / DPR 1 / 그림자 켬 / 기본 시점, 정지·회전·운행 각 3회. 실행별 중앙값의 중앙값은 정지·회전 전후 33.3ms, 운행 전후 33.4ms이며 운행 p95는 50.0→50.1ms다. 기본 호출 4,092→4,137회, 삼각형 877,662→890,294개다. 로프 버퍼 재사용·TubeGeometry 재생성 0회·오류 0건을 유지했다. `.shot-rope-performance/car-lock-before.json`, `car-lock-after.json`에 기록했다. 실기기 모바일 결과는 아니다.

- 카 도어 실사 구동부 교체(2026-09-20): 사용자 사진 112451/112453 및 104127 영상 설명에 따라 상부 취부판 뒤 모터·컨트롤러, 검은 구동벨트, 큰 풀리와 동축 로프 드럼, 안내 풀리·연동로프·문짝 클램프를 구성했다. `js/car-door-transmission.js`가 수치·경로·회전의 원본이며, 도면 232쪽의 긴 링크 2개와 고정 브라켓은 제거했다. 카도어락·클러치·승장문 결합과 독립 점검/바이패스는 유지한다. 상세는 [CAR-DOOR.md](CAR-DOOR.md)의 실사 구동 계약이다.
- 실사 구동 검증: `verify_car_transmission.mjs`의 101개 자세 회전비·클램프 이동·로프 재사용·안내 풀리 원판 관통 부재를 통과했다. 정면·사선·후면 모터·개방 PNG는 `.shot-car-transmission/`에서 확인했다. `verify_car_door.mjs`, `verify_car_wiring.mjs`, `verify_hall_manual.mjs`의 정상 개폐/잠금·배선·독립 승장문·바이패스·PC/터치 에뮬레이션 검사를 통과했고 브라우저 오류는 0건이다. 실제 모바일 조작·성능은 미검증이다. 다음은 사용자 화면 비교 후 카도어락 등 후속 부품 수정이며, 이번에는 그 세부 형상을 바꾸지 않았다. 커밋·푸시·배포하지 않았다.
- 실사 구동 전후 성능: Radeon Graphics / Chrome 149 / 1280×850 / DPR 1 / 그림자 켬 / 동일 기본 시점, 정지·회전·운행 각 3회. 실행별 중앙값의 중앙값은 정지·회전 전후 16.7ms, 운행 전후 33.3ms이며 운행 p95는 전후 33.4ms다. 변경 후 정지 1회는 33.2ms로 편차가 있었다. 기본 호출 4,058→4,092회, 삼각형 861,334→877,662개, 로프 버퍼 재사용 및 TubeGeometry 재생성 0회·오류 0건이다. `.shot-rope-performance/photo-drive-before.json`, `photo-drive-after.json`에 기록했다. 실제 모바일 결과는 아니다.

- 승장문 독립 점검 개방(2026-09-20): 고장 → 문 점검에서 대상 층·삼각키·0~100% 개방·관찰 위치를 제공한다. 열린 층은 카 접근·통과와 무관하게 유지하며, 승장문 BYPASS 후 INS ▲/▼를 누르는 동안 카를 움직여 에이프런·가이드슈·카도어를 관찰한다. 닫기·재잠금·BYPASS 해제 후 정상 운행으로 복귀한다. 상세는 [CAR-DOOR.md](CAR-DOOR.md)의 독립 점검 계약이다.
- 독립 점검 검증: `verify_hall_manual.mjs`의 부분/완전 개방 양방향 통과·문/카메라 유지·출발 차단·카문 이상 정지·포커스 상실/비상정지·정상 복귀와 터치 홀드 해제 통과. `verify_car_underbody.mjs`, `verify_car_door.mjs` 회귀 검사 통과, 브라우저 오류 0건. `.shot-hall-manual/` 화면과 결과를 확인했다. 실제 S22 조작·경보음·성능은 미검증이며 사용자 확인이 다음 작업이다. 커밋·푸시·배포는 하지 않았다.
- 독립 점검 전후 성능: Radeon Graphics / Chrome 149 / 1280×850 / DPR 1 / 그림자 켬 / 동일 기본 시점에서 정지·회전·운행 각 3회 측정했다. 운행 중앙값은 전후 약 33.3ms, p95 실행별 범위는 전후 33.4~50.0ms다. 정지·회전 중앙값은 변경 전 33.3ms, 변경 후 16.7~33.3ms로 실행 편차가 있어 개선 효과로 단정하지 않는다. 기본 호출 4,058회·삼각형 861,334개는 동일하고, 로프 버퍼 재사용·형상 재생성 0회·오류 0건을 유지했다. 원본은 `.shot-rope-performance/hall-manual-before.json`, `hall-manual-after.json`이며 실제 모바일 결과는 아니다.

- 배선 표현 정정·실내 상자 수정(2026-09-19, 아래 7개 글랜드 기록보다 우선): 사용자 정정에 따라 전체 보호관 표현을 검은 전선으로 바꾸고, 카탑 박스 아래에서만 한 묶음으로 모아 원형 보호 부시가 있는 지름 50mm 구멍 하나에 인입했다. 모서리·프레임 경로는 유지한다. 카 안의 큰 네모는 도어 `batchHardware()`가 인스턴싱 고정대의 원본 1m 상자를 일반 메시로 병합한 오류였다. 인스턴스/스킨 메시를 병합에서 제외해 수정했다.
- 정정 검증: `verify_car_wiring.mjs`에서 단일 구멍/보호 부시, 검은 전선 13개, 7회로의 구멍 내 종점, 천장·외판·플랫폼 간섭, 도어 고정대 인스턴싱 유지와 실내의 잘못 병합된 상자 부재를 확인했다. 박스 확대·실내 PNG를 직접 확인했고 `verify_car_door.mjs`, `verify_car_underbody.mjs`, `verify_safety_device.mjs`도 통과했다. 브라우저 오류 0건이며 실제 모바일 조작·성능 확인은 남아 있다. 커밋·푸시·배포는 하지 않았다.
- 정정 전후 성능: 동일 Radeon Graphics / Chrome 149 / 1280×850 / DPR 1 / 그림자 켬 / 기본 시점에서 각 3회 순차 측정했다. 정지·회전 중앙값은 전후 약 33.3ms, 운행 중앙값은 전후 약 33.4ms, 운행 p95 중앙값은 전후 약 50.0ms다. 호출 4,073→4,058회·삼각형 863,006→861,334개, 로프 버퍼 재사용·형상 재생성 0회·오류 0건을 유지했다. `.shot-rope-performance/wire-bundle-before.json`, `wire-bundle-after.json`에 기록했다. 실기기 모바일 결과는 아니다.

- 카 상·하부 배선 정리(2026-09-19): 사용자 표시 사진 224933/224945/2249481을 반영해 천장 중앙·대각 배선을 제거하고 토보드 밖 가장자리와 하부 프레임·외판 모서리 경로로 정리했다. `js/car-wiring.js`에서 직각 경로·보호관·인스턴싱 고정부·인입 좌표를 공유한다. 카탑 박스 하부에 실제 구멍 7개와 글랜드를 만들고 도어 제어반을 포함한 각 회로를 별도 인입했다. OPB 외판 통과부도 서비스 홀/부시로 처리했다. 상세는 [CAR-CONTROLS.md](CAR-CONTROLS.md)의 최신 배선 계약이다.
- 배선 검증: Live Server에서 `verify_car_wiring.mjs`의 고정 배선 13개 직각 경로·천장 중앙 비움·천장 간섭·외판/플랫폼/하중 취부선반 관통·7개 실제 인입구의 개별 연결을 통과했다. 보강채널을 통과하던 기존 안전장치 리드는 채널 밑으로 내렸다. `.shot-car-wiring/`의 상부/후면/하부/측면/박스 확대 화면을 확인했다. `verify_car_underbody.mjs`, `verify_safety_device.mjs`, `verify_car_door.mjs`도 통과했으며 운행·도어·바이패스·안전장치 트립/복귀와 카 추종을 유지한다. 브라우저 오류 0건, JavaScript 구문 및 diff 공백 검사 통과.
- 배선 전후 성능: Radeon Graphics / Chrome 149 / 1280×850 / DPR 1 / 그림자 켬 / 동일 기본 시점에서 다른 검증 브라우저를 종료하고 정지·회전·운행 각 3회 순차 측정했다. 세 상태 모두 전후 중앙값 약 33.3ms, 운행 p95 중앙값 약 49.9→50.0ms다. 기본 호출 4,076→4,073회, 삼각형 846,026→863,006개, 로프 형상 재생성 0회·버퍼 재사용 유지. 원본은 `.shot-rope-performance/wiring-before.json`, `wiring-after.json`이다. 실기기 성능으로 해석하지 않는다. 다음은 사용자 화면 확인 및 실제 S22 조작·성능 확인이다. 커밋·푸시·배포는 하지 않았다.

- 에이프런·바이패스 추가 검증(2026-09-19): 기존 구현을 유지하고 `verify_car_underbody.mjs`에 카문 우회 중 다른 층 승장 잠금 해제, 승장문 우회 중 카문 잠금 해제에 따른 정지·재출발 차단, 실제 비상정지 핸들러의 이동/경광등/음원 정지 검사를 보완했다. 터치 검증도 `SIMULATOR_URL`을 따르도록 수정했다. CDN 네트워크 허용 후 Live Server에서 전체 검사 통과, 브라우저 오류 0건이며 에이프런 장착·경광등 점등 캡처를 확인했다. 앱 구현 변경은 없으므로 기존 전후 성능 기록을 유지한다. 다음은 실제 S22 조작·음색/음량·성능 확인이다. 이번 후속은 커밋·푸시·배포하지 않았다.

- 에이프런·바이패스 재개 확인(2026-09-19): 저장된 구현을 유지하고 Live Server에서 `verify_car_underbody.mjs`를 다시 실행해 통과했다. CDN 접근 차단은 네트워크 허용 실행으로 해소했으며 브라우저 오류는 0건이다. 장착·경광등 점등 화면과 JavaScript 구문·Git diff 공백 검사를 확인했다. 실제 S22 조작·음색/음량·성능 확인은 여전히 남아 있다.
- 에이프런·바이패스 경광등(2026-09-19): 사용자 요청으로 `부품설계.pdf` 227/256페이지와 제공된 에이프런 설명 이미지를 반영했다. `js/car-underbody.js`에 수직부 750mm 에이프런·60° 하단 절곡·지지 브라켓, 오버로드와 에이프런 사이의 빨간 사각 CC27 경광등을 추가했다. 경광등 지지대는 플랫폼 보강채널 좌표에서 계산한다. 상세는 [CAR-REBUILD.md](CAR-REBUILD.md)의 에이프런·바이패스 계약이다.
- BYPASS 동작: 고장 메뉴에서 해제/승장문/카문 중 하나를 선택한다. 활성 시 INS로 전환하며 자동 운행·자동 문 개폐·AUT 복귀를 차단한다. 점검 이동 중 빨간 점멸과 일정 음높이의 반복 경보음을 내고 정지 시 끈다. 카문 바이패스에도 실제 두 문짝의 닫힘을 별도 감시한다. 두 문 동시 우회와 이동 중 모드 변경은 차단한다.
- 하부 전후 성능: 다른 검증 브라우저를 종료한 상태에서 수정 전 `index.html`·`js/ui.js`를 요청에만 적용한 기준본과 현재본을 순차 측정했다. Radeon Graphics / Chrome 149 / 1280×850 / DPR 1 / 그림자 켬 / 동일 기본 시점, 정지·회전·운행 각 3회다. 정지·회전 중앙값은 전후 33.3ms, 운행은 전후 33.3~33.4ms·p95 약 50~50.1ms다. 기본 호출 4,049→4,076회, 삼각형 844,966→846,026개이며 로프 버퍼 재사용·형상 재생성 0회·오류 0건을 유지했다. 기록은 `.shot-rope-performance/underbody-before.json`, `underbody-after.json`이다. 실제 모바일 성능 결과는 아니다.
- 하부 검증: `verify_car_underbody.mjs`에서 에이프런 폭·높이, 승장측 간극 약 27mm, 기본 최하층 피트 간극 약 973mm, 오버로드/에이프런 사이 배치, 자동 제어 차단, 우회 접점별 점검 이동·카문 벌어짐 시 정지, 경광등 켬/끔과 Web Audio 반복·정지, 터치 화면 선택을 확인했다. `.shot-car-underbody/`에 JSON과 장착·점등/소등·터치 PNG를 남긴다. 기존 카 도어 검증도 통과했다. 실제 S22 조작·음색/음량·성능은 미검증이며 다음 확인 항목이다. 이번 작업은 커밋·푸시·배포하지 않았다.

- 카 도어 후속 검증(2026-09-19): 작성돼 있던 `js/car-door.js`와 카 실·운행 연동을 유지하고 Live Server에서 검증을 마쳤다. 중단 원인인 `THREE is not defined`는 샌드박스의 CDN 요청 차단(`net::ERR_NETWORK_ACCESS_DENIED`)이었다. 네트워크 접근을 허용한 실행에서 정상 로드되므로 앱 라이브러리와 버전은 변경하지 않았다. 검증 도구에 요청 실패 URL·원인 출력을 추가했다.
- 카 도어 결과: `verify_car_door.mjs`의 해정/이동 각 101자세, 베인/롤러 접촉, 고정 링크 길이, 다른 층 문 유지, 실 수평 간극 30mm·문 하단 5mm, 층간 개방 차단, 정상 운행, 열림/닫힘 중 비상정지·복귀, 재잠금 후 후속 운행과 터치 에뮬레이션을 통과했다. 정면 닫힘/열림·오퍼레이터 사선·롤러 접촉·터치 PNG를 확인했고 브라우저 오류는 0건이다. `verify_overspeed.mjs --shadows --detail-view`도 트립·RST·구출 운전 통과. 결과는 `.shot-car-door/report.json`과 PNG, 상세 계약은 [CAR-DOOR.md](CAR-DOOR.md)다.
- 카 도어 현재본 성능: `verify_rope_performance.mjs car-door-resume`, Radeon Graphics / Chrome 149 / 1280×850 / DPR 1 / 그림자 켬 / 기존 기본 시점에서 각 3회 측정했다. 정지·회전 중앙값 33.3ms, 운행 중앙값 33.4~49.9ms·p95 50.1ms, 기본 호출 4,049회·삼각형 844,966개다. 로프 형상 재생성 0회·버퍼 재사용 정상, 오류 0건. 같은 세션의 추가 전 기준본과 비교하지 않아 성능 개선·저하를 단정하지 않는다. 원본은 `.shot-rope-performance/car-door-resume.json`이다. 다음은 실제 S22 이상 / Chrome에서 카 도어 개폐·이동·근접 관찰과 성능 확인이며, 터치 에뮬레이션은 실기기 검증을 대체하지 않는다. 이번 후속에서 커밋·푸시·배포는 하지 않았다.

- Live Server 후속 검증 완료(2026-09-19): `http://127.0.0.1:5500/index.html`에서 캐시 비활성화·새로고침 후 `verify_view_modes.mjs daylight-live`를 통과했다. 지면 간극 0.03m, 0.04/0.5/10m 관찰 거리의 near 0.002/0.01/0.1m, 6개 시점·배경 반복 복원·PC 더블클릭·터치 에뮬레이션 모두 정상이며 오류 0건이다. 전체 화면과 피트 기초 접합부 PNG를 확인했다. 검증 도구에 지면 간극 검사를 추가하고 클리핑 결과도 JSON에 남긴다. 기록은 `.shot-render-performance/daylight-live-views.json` 및 같은 접두사의 PNG다. 앞선 기록의 Live Server 미검증 항목은 해소됐다. 실제 S22 확인은 사용자 요청으로 보류하며, 이번 후속에서 커밋·푸시·배포는 하지 않았다.

- PC 전후 성능 비교 후속(2026-09-19): 사용자 요청으로 이번 후속은 PC 검증만 진행하며 실기기 확인·배포는 보류한다. 작업 파일은 유지하고 브라우저 요청에만 HEAD(`d969c22`)의 `js/environment.js`·`js/ui.js`를 적용해 기준본을 측정한 뒤 현재본을 순차 측정했다. 두 측정 모두 Radeon Graphics / Chrome 149 / 1280×850 / DPR 1 / 그림자 켬 / 카메라 `(18,7.152,21)`·타깃 `(0,7.152,0)`이며 정지·회전·운행 각 3회다. 실행별 중앙값의 중앙값은 정지 33.3→16.7ms, 회전 33.3→16.8ms, 운행 50→33.3ms, p95 중앙값은 각각 33.5→33.4 / 33.4→33.4 / 66.7→33.4ms다. 실행별 편차와 순차 측정의 영향을 포함하므로 배경 변경의 성능 향상으로 단정하지 않는다. 렌더 호출 3,953회·삼각형 823,106개는 전후 동일하고 브라우저 오류 0건·로프 버퍼 재사용도 통과했다. 원본은 `.shot-rope-performance/daylight-before-paired.json`, `daylight-after-paired.json`이다.

- 밝은 배경·겹침 보정 재개(2026-09-19): 남아 있던 `js/environment.js`의 하늘 배경·외부 지면 30mm 하향과 `js/ui.js`의 거리별 near 조정(0.002~0.1m)을 이어서 검증했다. `verify_view_modes.mjs`에 0.04/0.5/10m 관찰 거리의 클리핑 검사를 추가했다. 캐시 비활성화·새로고침한 로컬 HTTP 화면에서 배경 복원, 6개 시점, PC 더블클릭, 800×1280/1280×800/412×915 터치 에뮬레이션 검사를 통과했다. 전체 화면과 휴대폰 크기 근접 PNG를 확인했다. `verify_overspeed.mjs --shadows --detail-view`의 트립·RST·구출 운전도 통과했고 브라우저 오류는 0건이다. 화면 기록은 `.shot-render-performance/resume-sky-final-*`다.
- 현재 성능 기록: `verify_rope_performance.mjs daylight-resume`에서 PC 1280×850·DPR 1·그림자 켬으로 정지/회전/운행을 각 3회 측정했다. 실행별 중앙값은 정지·회전 16.7~33.3ms, 운행 33.3~49.9ms이며 운행 p95는 50~50.1ms다. 호출 3,953회·삼각형 823,106개, 로프 형상 재생성 0회·버퍼 재사용 통과. 기록은 `.shot-rope-performance/daylight-resume.json`이다. 이번 재개에서는 변경 전후 동시 조건 비교를 하지 않아 성능 개선으로 판정하지 않는다. Live Server 및 실제 S22/Chrome에서 연속 확대·회전 시 깜빡임과 성능 확인은 남아 있다. 다음 작업은 이 확인과 기존 통합 점검이며, 이번 재개에서 커밋·푸시는 하지 않았다.

- 최신 배포 운영 합의(2026-09-19, 이전 보류 지시보다 우선): 수정·검증 후 커밋·푸시하여 자동 배포를 재개한다. 배포 삭제는 자동 실행하지 않고 사용자가 “5개 빼고 삭제”라고 요청할 때만 한다. 총 5개를 남기되 `agwrtmqr3`(5월 11일), `ec4kw7x2u`(8월 9일), `f54ocyc82`(9월 6일)는 변화 과정 비교용으로 반드시 보존한다. 나머지 2개는 현재 서비스 배포와 월별 변화를 볼 수 있는 대표 배포를 우선한다. 현재 Vercel 자체 Retention은 별도 설정이며 이번 합의만으로 변경한 것은 아니다. 사용자에게 CLI 삭제 후 사용량이 크게 줄었다는 확인을 받았다. 상부 전면벽 수정도 검증을 마쳤으며 이번 커밋으로 배포한다.

- Vercel 배포 정리(2026-09-19): 사용자 로그인 및 명시적 선택 후 `47iit3r1z`, `d5cgu0ymr`, `456x58nge` 3개를 CLI로 삭제했다. 변화 과정 비교용 `agwrtmqr3`(최초), `ec4kw7x2u`(중간), `f54ocyc82`(최근)와 현재 서비스 `8zynpc3jm`을 보존한다. 이 4개는 사용자 요청 없이 정리하지 않는다. 삭제 후 목록 4개와 `simulator-project-ecru.vercel.app`이 `8zynpc3jm`의 Ready 배포에 연결됨을 확인했다. 영구 삭제·사용량 감소까지 확인한 것은 아니다. 상부 벽 수정은 로컬에 있고 새 푸시·배포는 아직 하지 않았다.

- 공중 지반 배경(2026-09-19): 사용자 요청으로 지평선까지 퍼지던 280m 지면 슬래브를 없애고, 보도블록 광장 ∪ 승강로 footprint 를 받치는 **네모난 지반 덩어리** 하나만 남겼다. 1차 시안의 뾰족한 천공섬·떠다니는 바위는 “섬보다 네모 구조물”이라는 지시로 폐기하고, 옆면이 수직 절벽으로 34m 내려가다 아래쪽에서 안개색에 녹는 형태(`makePlinthGeometry`)로 바꿨다. 상면은 기존 지면 레벨(Y0-0.03)을 유지해 광장·계단·나선 램프 높이는 그대로이며, 흙·잔디 테두리는 `PLINTH_MARGIN` 4.0m, 광장이 없어 좁아 보이던 승강로 후면만 `PLINTH_BACK_MARGIN` 7.5m 로 더 뺐다(지반 21.2 × 27.8m). 구름은 `outdoorGround` 로 옮겨 두 배경 모드에서 모두 보이고, 덩어리가 아니라 반투명(0.30) 안개로 엷게 깔린다. 땅이 없어졌으므로 지형·풀밭·들꽃·개울·주변 건물은 `BUILD_GROUND_SCENERY = false` 로 생성하지 않는다(코드는 보존, 한 줄로 복구). ‘주변 풍경’ 토글은 하늘 돔·야외 조명·노출만 바꾸며, 하늘 돔은 지평선 아래도 보이므로 그라데이션을 다시 잡고 `toneMapped = false` 로 잿빛 바램을 막았다. 검증은 `tools/verify_sky_island.mjs`(광장 81점·승강로 4모서리 지지, 상면 30mm, 구름 배치, 5각도+상세 모드 스크린샷)와 `verify_view_modes.mjs plinth`다.
- 상부 전면벽 보완: 사용자가 상부 높이는 적절하다고 확인했고, 로비 캐노피와 기계실 슬래브 사이에 누락된 승강로 전면벽을 요청했다. `shaftOverheadFrontWall`을 기존 벽의 폭·두께·재질로 연결했다. 로비 천장 높이와 카 상부 여유는 유지한다. `verify_overhead.mjs`에 벽 하단·상단·내측면 접합 검증을 추가했다.
- 벽 검증: Live Server에서 최상층 후면 화면을 확인하고 접합·기존 상부 치수 검증을 통과했다. 동일 PC·1100×1000·동일 최상층 시점에서 벽 숨김/표시를 각 3회 비교한 정지 프레임 중앙값은 모두 약 16.7ms, 호출 2,627→2,633회·삼각형 564,058→564,070개다. 실기기 검증은 하지 않았다. 로컬 결과는 `.shot-overhead/report.json`과 `after.png`다.
- 배포 운영 변경: 사용자 요청으로 Vercel 저장공간 정리 가능 여부를 확인할 때까지 새 푸시·배포를 보류한다. 사진 사용량은 Deployment Storage 7.84/10GB. 현재 PC에 Vercel CLI 인증 정보가 없어 삭제는 실행하지 않았다. 사용자가 `npx vercel login` 후 프로젝트 대시보드 주소를 제공하면 현재 서비스 배포를 보존하며 과거 배포·보관 설정을 확인한다. `remove --yes`는 확인 생략 옵션이며 즉시 영구삭제 옵션으로 안내하지 않는다. 정리 가능 여부가 확인되기 전에는 사용자가 배포를 담당한다.

- 상부 보정 성능: 같은 세션에서 변경 전/후 각 3회 재측정했다. Radeon Graphics / Chrome 149 / 1280×850 / DPR 1 / 그림자 켬, 카메라 `(18,6.724,21)`·타깃 `(0,6.724,0)` 고정. 정지·회전·운행 중앙값은 전후 약 33.3ms, p95는 정지·회전 약 33.4ms 및 운행 50ms로 같은 수준이다. 기본 호출 3,928→3,944회, 삼각형 819,266→823,058개다. 원본 `.shot-rope-performance/overhead-before.json`, `overhead-after.json`. 이전 날짜의 16.7ms 기록과 직접 비교하지 않는다. 모바일 성능은 사용자 후속 확인이 필요하다.

- 2단계 전 상부 공간 보정: 사용자 사진의 최상층 정상 정차 장면을 기준으로 `OVERHEAD`를 3.70→4.77m로 복원했다. 이 상수는 최상층 바닥부터 기계실 기준 바닥까지이며 슬래브 하면까지는 4.525m다. 카 천장 위 공간은 1.10→2.17m, 난간 상단 위 공간은 약 1.247m다. 층 정지점·카/균형추 이동 범위·종단 스위치는 유지하고 기계실·권상/편향/조속기 시브·레일 상단·천장 배선을 연동시켰다. 기존 6단 브라켓에 상부 지지 한 단을 추가했다. 완충기 압축 조건의 검사기준 적합성 판정은 이번 범위가 아니다.
- 상부 검증: `verify_overhead.mjs`에서 전후 기계실 메시 538개가 동일하게 +1.07m 이동하고 카·균형추·난간·정션박스·종단 스위치가 불변임을 확인했다. Live Server 전후 스크린샷은 `.shot-overhead/`, 권상로프 25개 위치의 기존 형상 대비 최대 오차 3.32e-9m와 왕복 운행 통과, 이동케이블·종단 검사 54/54 통과, 과속 시연·RST·구출 운전 통과. 케이블 검사 도구는 다른 도구와 같은 `--enable-gpu`로 셰이더 초기화 오류를 해결했다. 실기기 최상층 화면·조작 확인은 남아 있다.

- 사용자 후속 확인: 배포된 확대·더블탭 카메라 개선에 대해 “양호해 잘되었어”라는 확인을 받았다. 조작감 확인으로 기록하며, 기기별 FPS 측정이나 모든 안전장치 검증 완료를 뜻하지 않는다. 다음은 기본 운행·안전장치·표시 속도·소리의 통합 점검이다.

- 카메라 검증 기록: Live Server 캐시를 비운 `verify_view_modes.mjs camera-live`에서 6개 프리셋·배경 복원·PC 더블클릭·800×1280/1280×800/412×915 터치 더블탭·핀치 구분을 통과하고 근접 화면을 확인했다. `verify_overspeed.mjs --shadows --detail-view` 통과. `verify_rope_performance.mjs camera-after`의 동일 PC·1280×850·픽셀 비율 1·그림자 켬·3회 측정에서 정지/회전 중앙값 16.7ms, 운행 33.3ms, p95 33.4ms로 직전 studio-after 기록과 같은 수준이다. 호출 3,928회·삼각형 819,266개, 브라우저 오류 0건. 실기기 성능 결과를 뜻하지 않는다.

- 모바일 카메라(2026-09-19): 사용 기준을 Galaxy S22 이상 / Chrome으로 변경한다. 사용자 확인으로 S22+는 원활하며 S7 FE는 저성능 참고 기기로 둔다. 일반 카메라 최소 거리 0.04m, 근접 클리핑 0.002m를 허용하고 부품 더블클릭·더블탭으로 표면 앞까지 부드럽게 접근한다. 직접 조작하면 접근을 중단하고 전체 운행 버튼으로 복귀한다. 배경·숨긴 부품은 제외하며 핀치·드래그·비상 열쇠·과속 시연과 입력을 분리한다. PC 및 3종 화면 크기의 터치 에뮬레이션에서 접근·핀치 구분·프리셋 복귀와 과속 시연·RST·구출 운전을 검증했다. 실제 S22에서 새 카메라 조작 확인은 남아 있다.

- 가독성 조정(2026-09-19): 사용자의 ‘흰 배경·흰 구조물이 겹쳐 보인다’는 피드백을 반영했다. 기본 관찰 배경은 청회색 그라데이션, 바닥은 중간 명도 청회색으로 조정하고 노출과 주변광·보조광을 줄여 구조물의 면·윤곽과 노랑/파랑 안전색을 구분한다. 4×256 배경 텍스처를 재사용하며 추가 3D 배경·후처리는 없다. ‘주변 풍경’은 원래 바닥·하늘·안개·노출·조명까지 복원한다. 카 내부 프리셋은 문 안쪽, 피트는 난간을 피한 후면 시점으로 보정했다. 전체 운행·카 내부는 세로 화면 거리 확대에서 제외해 과도한 후퇴와 벽 밖 이동을 방지한다.
- 가독성 검증: Live Server 캐시 비운 새로고침 후 `verify_view_modes.mjs studio`로 6개 시점·배경/조명 반복 복원·부품 표시 보존과 800×1280·1280×800·412×915 터치 화면을 확인했다. 프리셋 검증은 GSAP의 시작 대기 중인 트윈까지 종료를 기다린 뒤 촬영한다. `verify_shadow_cache.mjs studio`의 정지/시점 이동 재계산 0회, 변경 6종 그림자 픽셀 차이 0. 같은 PC·해상도·시점·그림자 조건의 각 3회 전후 결과는 정지/회전 중앙값 약 16.7ms, 운행 약 33.3ms, p95 약 33.4ms로 전후 동일하다. 배경 패스 추가로 호출 3,927→3,928회, 삼각형 819,264→819,266개. 원본은 `.shot-rope-performance/studio-before.json`, `studio-after.json`, 화면·전환 검증은 `.shot-render-performance/studio-views.json`과 PNG. 실기기 성능 검증은 여전히 남아 있다.
- 화면 구성 구현(2026-09-18): 기본 배경은 승강기·진입 광장·평면 바닥만 표시한다. `outdoorLandscape` 아래에 지형·풀·꽃·개울을 묶고 주변 건물·하늘 돔과 함께 숨긴다. `setDetailedBackground()`와 카메라 메뉴의 ‘주변 풍경’으로 원래 풍경·바닥 재질·하늘·안개를 복원할 수 있다. 풍경 형상은 최초 로딩에 생성하며 이번 작업은 렌더 비용 개선이지 초기 생성·다운로드 비용 개선은 아니다. 부품·로프·센서의 상세 형상과 표시 상태는 유지한다.
- 관찰 화면: 카메라 메뉴를 ‘전체 운행’과 ‘부품 관찰’로 구분했다. 기존 기계실·피트·카 내부에 카 상부·조속기 프리셋을 추가하고 선택 상태를 표시한다. 관찰 시 근접 확대를 허용하고 전체 운행으로 돌아오면 원래 근접 제한을 복원한다. 프리셋 연속 선택은 이전 카메라 이동을 취소하며 과속 자동 시연 중에는 관찰 프리셋이 카메라를 덮어쓰지 않는다. 세로 화면 프리셋은 시선 방향을 유지하며 거리를 늘린다.
- 화면 구성 검증·측정: `verify_view_modes.mjs`로 배경 반복 전환, 부품 표시 보존, 6개 카메라 프리셋과 전체 복귀를 확인했다. Live Server 캐시 비운 새로고침과 800×1280·1280×800·412×915 터치 에뮬레이션 메뉴·스크린샷 확인, 왕복 운행 3회, `verify_overspeed.mjs --shadows --detail-view` 트립·RST·구출 운전 통과, 브라우저 오류 0건. Radeon Graphics / HeadlessChrome 149 / 1280×850 / 픽셀 비율 1 / 같은 시점·그림자, 각 3회 전후 측정에서 기본 호출 4,169→3,927회, 삼각형 약 183만→82만 개(약 55% 감소). 실행 중앙값의 중앙값은 정지·회전 33.3→16.7ms, 운행 33.3→33.3ms, p95 중앙값은 모두 약 33.4ms다. 로컬 결과는 `.shot-rope-performance/views-before.json`, `views-after.json`, `.shot-render-performance/live-views.json`과 관련 PNG다. 터치 에뮬레이션은 S7 FE 실기기 성능 검증이 아니다.
- 레일 체결부 최적화(2026-09-18): `js/environment.js`의 `batchGuideRailFasteners()`가 13K/8K 레일 GLB를 로드할 때 같은 부모의 고정 볼트·너트·나사 끝만 재질·렌더 속성별로 묶는다. 16개 모듈의 메시 416→48개, 원본 GLB와 `T_Rail_*` 본체·이음판은 유지한다. 묶음 이름의 `FP_` 접두어로 최상단 이음부 숨김을 유지하고 원래 부품명은 `sourceRanges`에 남긴다. 양쪽·상하 모듈은 묶음 형상을 공유한다.
- 레일 체결부 검증: `tools/verify_rail_fasteners.mjs`로 16개 모듈·최상단 4개·이름 있는 레일 본체 16개를 원본과 비교했다. 표시 삼각형 18,384개 동일, 최대 위치 오차 2.21e-7, 법선 오차 1.65e-8 이하, UV 오차 0. 원본/묶음 근접 화면과 Live Server 캐시 비운 새로고침도 확인했다. 왕복 운행 3회 및 `verify_overspeed.mjs --shadows`의 과속 단면·트립·RST·구출 운전 통과, 브라우저 오류 0건.
- 레일 체결부 전후 측정: Radeon Graphics / HeadlessChrome 149 / 1280×850 / 픽셀 비율 1 / 그림자 캐시 켬 / 동일 기본 시점, 각 3회. 기본 렌더 호출 4,445→4,169회(6.2% 감소), 삼각형 수 동일. 각 실행 중앙값의 중앙값은 정지 33.3→33.3ms, 시점 이동 33.4→33.3ms, 운행 33.4→33.3ms이며 FPS 향상은 미미하다. p95의 중앙값은 정지 33.4→33.4ms, 시점 이동 50→33.4ms, 운행 50.1→50ms. 원본 결과는 `.shot-rope-performance/fishplate-before.json`, `fishplate-after.json`, 형상 결과는 `.shot-render-performance/live-rail-fasteners.json`. 실제 S7 FE 모바일 결과는 아직 확인하지 않았다.
- 실기기 기준 확정(2026-09-18): 사용자 테스트 환경은 Galaxy Tab S7 FE / Chrome, 비교 기기는 Galaxy S22+ / Chrome이다. 기존 배포에서 S22+는 상대적으로 원활하고 S7 FE는 느림·버벅임을 보고했다. 목표 FPS 수치는 아직 합의하지 않았다.
- 그림자 캐시 구현·PC 검증(2026-09-18): `js/environment.js`의 `installShadowCache()`를 `index.html`에서 설치한다. 주 화면의 그림자 생성 부품·조명·월드 행렬·표시 상태·형상 버퍼·재질을 비교해 변화가 없으면 그림자맵을 재사용한다. 움직임·지연 로딩·단면 표시 변경은 즉시 갱신하며 과속 확대 화면은 캐시에 관여하지 않는다. 2048×2048 그림자·PCFSoft 품질과 부품 형상은 유지했다. `verify_shadow_cache.mjs`의 정지/시점 이동 재계산 0회, 변경 6종 즉시 갱신 및 강제 재계산 그림자맵과 픽셀 차이 0을 확인했다. 설치된 Live Server에서 캐시를 비운 새로고침 후에도 통과했으며 화면을 확인했다. 왕복 운행 3회와 `verify_overspeed.mjs --shadows`의 트립·RST·구출 운전 통과, 브라우저 오류 0건.
- 그림자 캐시 전후 측정: Radeon Graphics / HeadlessChrome 149 / 1280×850 / 픽셀 비율 1 / 동일 그림자·기본 시점, 각 3회. 각 실행 중앙값의 중앙값은 정지 49.9→33.3ms, 시점 이동 50→33.3ms, 운행 50→50ms다. p95는 약 50ms로 남았다. 원본 결과는 `.shot-rope-performance/shadow-before.json`, `shadow-after.json`, 그림자 검증은 `.shot-render-performance/live-shadow-cache.json`이다. 이번 개선은 정지·시점 이동에 해당하며 S7 FE 실기기 개선은 사용자 재테스트가 필요하다.
- 모바일 테스트 배포(2026-09-18): 사용자 요청으로 `dc97976`을 `origin/main`에 푸시했고 GitHub의 Vercel Production 배포 성공을 확인했다. 로컬 참고 APK·jadx·Python 캐시는 커밋에서 제외했다.
- 렌더링 추가 진단(2026-09-18): `tools/verify_render_performance.mjs shadow-grass-review --detailed`로 같은 PC·1280×850·픽셀 비율 1·기본 시점에서 그림자/풀밭/배경/지면 제외를 각 3회 측정했다. 기준 프레임 중앙값 약 49.9~50ms, 그림자 제외는 세 번 모두 약 33.3ms이며 p95는 약 50ms로 남았다. 풀밭 제외는 삼각형 1,826,005→882,005개지만 중앙값 33.5/49.9/49.9ms로 개선이 일관적이지 않았다. 브라우저 오류 0건. 결과는 로컬 `.shot-render-performance/shadow-grass-review.json`. 앱 품질 설정은 변경하지 않았으며 다음 구현은 그림자 갱신·범위 비용을 우선 확인한다. 모바일 성능은 아직 미검증이다.
- 이전 재개 메모(2026-09-18): 사용자 요청으로 렌더링 추가 개선을 재개하여 아래 2차 작업을 완료했다. 로프·레일 브라켓 완료 작업을 다시 시작하지 않는다. 당시에는 기종 미응답·미커밋 상태였으며, 이후 배포와 기기 확정은 위 기록을 따른다.
- 완료: 웹 운영 방향과 개발 순서 합의, 불필요한 검증·추출 이미지 810개 정리, 최적화 계획 작성.
- 기존 과속 검증: `node tools/verify_overspeed.mjs`로 정지·로프 파지·쐐기 상승·RST·구출 운전 통과. 모바일 성능 검증을 의미하지 않는다.
- 로프 최적화 구현: `js/elevator.js`에서 5가닥이 형상 하나를 공유하고 위치·법선 버퍼를 갱신한다. 정지 중에는 동일 위치의 버퍼 갱신을 생략한다. 경로·UV·시브 연동과 기존 미커밋 변경을 유지한다.
- PC 전후 측정(2026-09-18 저장 결과): `tools/verify_rope_performance.mjs`, Radeon Graphics / HeadlessChrome 149 / 1280×850 / 픽셀 비율 1 / 그림자 켬 / 동일 기본 시점. 준비 후 정지·시점 이동·운행을 각 3회 측정했다. 로프 갱신 중앙값 2.6→0.6ms, p95 3.1→0.8ms, 120회 갱신 중 TubeGeometry 생성 600→0개. 전체 프레임 중앙값은 약 33.3~33.4ms로 큰 변화가 없고 기본 렌더 호출은 5,284회다. 전체 60fps 달성으로 해석하지 않는다. 원본 결과는 로컬 `.shot-rope-performance/before.json`, `after.json`이다.
- 로프 회귀 검증(2026-09-18 재실행 통과): `tools/verify_wire_rope.mjs`의 25개 위치·5가닥 비교에서 최대 위치 오차 약 3.32e-9, 법선 오차 0. 인덱스·UV·경계·버퍼 재사용·정지 시 갱신 생략 통과. 왕복 운행을 포함한 보고서의 브라우저 오류는 없다. 시브 스크린샷도 확인했다. 최적화 후 과속 검증도 재실행하여 트립·RST·구출 운전 통과와 브라우저 오류 0건을 확인했다.
- 렌더링 1차(2026-09-18): `tools/verify_render_performance.mjs`로 기본·배경 제외·지면 제외·그림자 제외 비용을 분리 측정했다. `js/environment.js`의 `batchRailBracket()`은 고정 레일 브라켓만 재질별로 묶는다. 6개 설치 그룹과 위치·userData를 유지하고 GLB 원본은 수정하지 않았다. 원본 메시 이름은 묶음의 `userData.sourceRanges`에 보존한다. 브라켓 메시 186→30개, 기본 화면 렌더 호출 5,284→5,128회(약 3% 감소), 삼각형 수 동일. 같은 실행 조건의 프레임 중앙값은 전후 약 50ms여서 FPS 개선은 확인되지 않았다. 이전 로프 측정의 약 33ms와 직접 비교하지 않는다. 원본 결과는 로컬 `.shot-render-performance/before.json`, `after.json`이다.
- 렌더링 1차 검증: `node tools/verify_rail_bracket_batch.mjs` 통과. 6개 브라켓의 6,840개 삼각형을 원본과 비교하여 최대 위치 오차 1.27e-8, 법선 오차 2.29e-8 이하, UV 오차 0을 확인했다. 원본·묶음 근접 렌더의 형상·설치 부품 상대 위치가 같다. `node tools/verify_overspeed.mjs`로 단면 숨김·복귀와 과속 정지·RST·구출 운전도 통과했다. 브라우저 오류 0건.
- 렌더링 2차: 카 체대·플랫폼·난간과 승장 실 서포트·실·토가드·삼방틀의 고정 메시 796→113개. `js/environment.js`의 `batchStaticChildren()`을 `js/elevator.js`의 해당 조립 함수에서만 호출한다. 이름·userData·자식이 있는 메시, 숨김·투명 부품은 제외하고 부모 로컬 좌표로 묶는다. 개별 가동부·센서·표시기 텍스처와 부모 그룹의 이동·표시 계약을 유지한다. 기존 레일 브라켓도 삼각형 병합 계산만 공용 `mergeStaticMeshBucket()`을 사용한다.
- 렌더링 2차 전후 측정: Radeon Graphics / Chromium 149 / 1280×850 / 픽셀 비율 1 / 그림자 켬 / 동일 기본 시점, 정지·시점 이동·운행 각 3회. 기본 렌더 호출 5,128→4,445회(13.3% 감소), 삼각형 수 동일. 각 실행의 프레임 중앙값을 다시 중앙값으로 집계하면 세 시나리오 모두 약 33.3ms로 동일하다. 각 실행 p95의 중앙값은 정지 50→33.5ms, 시점 이동 50→33.4ms, 운행 50→33.5ms다. 60fps 달성이나 실제 모바일 결과를 의미하지 않는다. 원본은 로컬 `.shot-rope-performance/static-before.json`, `static-after.json`이다.
- 렌더링 2차 검증: `tools/verify_static_batches.mjs`에서 35개 조립 그룹의 원본 32,808개 삼각형과 비교했다. 위치 오차 1.32e-7 이하, 법선 오차 3.08e-8 이하, UV 오차 0, 기존 이름 있는 부품 유지. 카·승장 원본/묶음 화면도 확인했다. 왕복 운행 3회, `verify_overspeed.mjs`, `verify_interlock_field.mjs`, `verify_rail_bracket_batch.mjs` 통과. 설치된 Live Server에서 캐시를 비운 새로고침 후 기본 화면·113개 묶음·로프 5개·승장 4층·IDLE·브라우저 오류 0건을 확인했다. 로컬 결과는 `.shot-render-performance/static-geometry.json`, `static-live-server.json` 및 같은 폴더의 PNG다.
- 다음 작업: 카메라 개선의 사용자 확인을 받았으므로 위 6번의 기본 운행·안전장치·표시 속도·소리 통합 점검을 진행한다. S22 이상 / Chrome 기준을 유지한다. 통합 점검 후 1단계를 마무리하며, 2단계 추가 부품·기능은 사용자 요청에 따라 진행한다.
- 단계 2~4를 자동으로 시작하지 않는다. 현재 사용자 요청과 이 문서의 순서를 함께 따른다.

## 문서 운영

- `AGENTS.md`: 공통 작업 규칙과 이 문서의 진입 링크.
- `README.md`: 현재 단계 요약과 이 문서의 링크. 단계 전환 시 함께 갱신한다.
- `PLAN.md`: 현재 작업 하나의 로컬 상세 계획. Git 제외. 작업 완료 후 삭제하거나 다음 작업 계획으로 교체한다.
- 부품별 기술 문서: 실제 구현이 바뀐 부분만 갱신한다. 전체 로드맵을 복사하지 않는다.
- 인계 시 이 문서의 완료 상태·남은 문제·다음 작업을 갱신한다. 다른 PC에서 로컬 계획 없이도 현재 위치를 알 수 있어야 한다.
- 새 부품과 기능을 추가할 때도 성능 기준을 다시 확인한다. 상세 형상 추가로 최적화 성과가 사라지지 않도록 한다.
