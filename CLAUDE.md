# Claude Code 프로젝트 진입점

작업 전에 루트 `AGENTS.md`를 읽고 공통 규칙을 따른다.
프로젝트 구조와 기능별 수정 위치는 `README.md`를 참고한다.
Blender 작업은 `blender/BLENDER-WORKFLOW.md`를 추가로 읽는다.

## 기본 동작

- 일반 요청은 Auto 모드에서 기존 코드를 확인한 뒤 바로 분석·수정·검증한다.
- 스크린샷의 색칠, 선, 원, 메모는 사용자가 지정한 수정 범위로 해석한다.
- 사용자가 계획을 요청하거나 `PLAN.md`를 읽으라고 명시한 경우에만 계획 기반으로 작업한다.
- 루트에 `PLAN.md`가 있다는 이유만으로 현재 요청과 무관한 계획을 자동 실행하지 않는다.
- 특정 모델을 강제하지 않는다.
